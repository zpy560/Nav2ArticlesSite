import{_ as s,o as e,c as i,a2 as n}from"./chunks/framework.C0KMm31j.js";const l="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_01_boundary.png",t="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_02_lifecycle.png",p="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_04_through_poses.png",o="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_03_single_pose.png",h="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_05_plugin_selection.png",r="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_06_service_params.png",y=JSON.parse('{"title":"一个目标点要闯过多少关？逐函数拆解 Nav2 Planner Server 的 21 段数据流","description":"给 Nav2 一个目标点，真正负责“算路”的并不是 plannerserver 本身。","frontmatter":{"title":"一个目标点要闯过多少关？逐函数拆解 Nav2 Planner Server 的 21 段数据流","description":"给 Nav2 一个目标点，真正负责“算路”的并不是 plannerserver 本身。","date":"2026-07-24","section":"二 · 全局规划与路径搜索","readingTime":27,"source":"2026-07-24/nav2_planner_source_walkthrough.md","status":"published","cover":"/article-assets/2026-07-24/nav2_planner_source_01_boundary.png","head":[["link",{"rel":"canonical","href":"https://zpy560.github.io/Nav2ArticlesSite/nav2/nav2-planner-source-walkthrough"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:title","content":"一个目标点要闯过多少关？逐函数拆解 Nav2 Planner Server 的 21 段数据流"}],["meta",{"property":"og:description","content":"给 Nav2 一个目标点，真正负责“算路”的并不是 plannerserver 本身。"}],["meta",{"property":"og:url","content":"https://zpy560.github.io/Nav2ArticlesSite/nav2/nav2-planner-source-walkthrough"}],["meta",{"property":"og:image","content":"https://zpy560.github.io/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_01_boundary.png"}],["meta",{"property":"article:published_time","content":"2026-07-24"}],["meta",{"name":"twitter:title","content":"一个目标点要闯过多少关？逐函数拆解 Nav2 Planner Server 的 21 段数据流"}],["meta",{"name":"twitter:description","content":"给 Nav2 一个目标点，真正负责“算路”的并不是 plannerserver 本身。"}],["meta",{"name":"twitter:image","content":"https://zpy560.github.io/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_source_01_boundary.png"}]],"prev":{"text":"一条参数命令，如何让 Nav2 当场“换脑”？逐行拆解 PlannerServer 动态回调","link":"/nav2/nav2-planner-dynamic-parameters-callback"},"next":{"text":"从机器人脚下掀起一场“势能洪水”：Nav2 NavFn 如何把 Costmap 变成路径？","link":"/nav2/nav2-navfn-planner-core-algorithm"}},"headers":[],"relativePath":"nav2/nav2-planner-source-walkthrough.md","filePath":"nav2/nav2-planner-source-walkthrough.md","lastUpdated":null}'),c={name:"nav2/nav2-planner-source-walkthrough.md"};function d(k,a,F,g,u,_){return e(),i("div",null,[...a[0]||(a[0]=[n('<h1 id="一个目标点要闯过多少关-逐函数拆解-nav2-planner-server-的-21-段数据流" tabindex="-1">一个目标点要闯过多少关？逐函数拆解 Nav2 Planner Server 的 21 段数据流 <a class="header-anchor" href="#一个目标点要闯过多少关-逐函数拆解-nav2-planner-server-的-21-段数据流" aria-label="Permalink to &quot;一个目标点要闯过多少关？逐函数拆解 Nav2 Planner Server 的 21 段数据流&quot;">​</a></h1><p>给 Nav2 一个目标点，真正负责“算路”的并不是 <code>planner_server</code> 本身。</p><p><code>nav2_planner</code> 更像一座全局规划调度中心：它管理 Global Costmap 和 Lifecycle，接收单目标或多目标 Action，把位姿转换到统一坐标系，选择已经加载的 GlobalPlanner 插件，再把插件返回的 <code>nav_msgs/Path</code> 交给上层导航流程。</p><p>这篇文章基于当前工程中的 <code>nav2_planner 1.1.20</code>、现用 <code>regulated_modules.yaml</code> 和本包测试代码，完整覆盖 <code>main.cpp</code> 与 <code>planner_server.cpp</code> 的 21 个生产函数。每个函数都按作用、输入、输出、回调位置、共享状态和包外调用展开，同时保留可直接落地的配置、调用、切换与排障方法。</p><h2 id="一、先把职责边界说清楚" tabindex="-1">一、先把职责边界说清楚 <a class="header-anchor" href="#一、先把职责边界说清楚" aria-label="Permalink to &quot;一、先把职责边界说清楚&quot;">​</a></h2><h3 id="_1-1-planner-server-负责什么" tabindex="-1">1.1 Planner Server 负责什么 <a class="header-anchor" href="#_1-1-planner-server-负责什么" aria-label="Permalink to &quot;1.1 Planner Server 负责什么&quot;">​</a></h3><p>Planner Server 对外提供两类路径计算 Action、一个路径有效性 Service 和一个路径可视化 Topic：</p><ul><li><code>/compute_path_to_pose</code>：从起点规划到单个目标。</li><li><code>/compute_path_through_poses</code>：依次经过多个目标点。</li><li><code>/is_path_valid</code>：检查现有路径的剩余部分是否撞上当前 Costmap 障碍。</li><li><code>/plan</code>：发布最近一次成功路径，供 RViz 或调试节点观察。</li></ul><p>它还负责管理规划插件的 Lifecycle，在请求进入时取得机器人当前位姿，等待 Global Costmap 更新，把起点和目标转换到 Costmap 的全局坐标系，并统一处理取消、失败、结果和耗时告警。</p><h3 id="_1-2-planner-server-不负责什么" tabindex="-1">1.2 Planner Server 不负责什么 <a class="header-anchor" href="#_1-2-planner-server-不负责什么" aria-label="Permalink to &quot;1.2 Planner Server 不负责什么&quot;">​</a></h3><p>它不实现 Dijkstra、A*、Hybrid-A*、State Lattice 或 Theta*，也不发布 <code>cmd_vel</code>。</p><p>真正的搜索发生在包外插件的：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nav2_core</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GlobalPlanner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createPlan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(start, goal)</span></span></code></pre></div><p>路径跟踪由 Controller Server 完成，路径平滑由 Smoother Server 完成，周期重规划由行为树或本工程的 <code>regulated_navigator</code> 调度。<code>expected_planner_frequency</code> 不会在 Planner Server 内创建重规划定时器。</p><p><img src="'+l+`" alt="Planner Server 使用边界"></p><h3 id="_1-3-一次请求的完整数据流" tabindex="-1">1.3 一次请求的完整数据流 <a class="header-anchor" href="#_1-3-一次请求的完整数据流" aria-label="Permalink to &quot;1.3 一次请求的完整数据流&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BT／Simple Commander／自研 Navigator</span></span>
<span class="line"><span>                ↓ Action Goal</span></span>
<span class="line"><span>         Planner Server</span></span>
<span class="line"><span>                ↓ 等待 Costmap current</span></span>
<span class="line"><span>        Robot Pose／请求起点</span></span>
<span class="line"><span>                ↓ TF 到 global_frame</span></span>
<span class="line"><span>      已加载 GlobalPlanner 插件</span></span>
<span class="line"><span>                ↓ createPlan()</span></span>
<span class="line"><span>           nav_msgs/Path</span></span>
<span class="line"><span>          ↙              ↘</span></span>
<span class="line"><span> Action Result          /plan</span></span></code></pre></div><p>理解这个边界后，规划失败就可以分成接口、Lifecycle、TF、Costmap、插件选择和算法结果几个层面，而不是把所有问题都归结为“A* 算不出来”。</p><h2 id="二、运行前必须满足的四个条件" tabindex="-1">二、运行前必须满足的四个条件 <a class="header-anchor" href="#二、运行前必须满足的四个条件" aria-label="Permalink to &quot;二、运行前必须满足的四个条件&quot;">​</a></h2><h3 id="_2-1-planner-server-必须处于-active" tabindex="-1">2.1 Planner Server 必须处于 active <a class="header-anchor" href="#_2-1-planner-server-必须处于-active" aria-label="Permalink to &quot;2.1 Planner Server 必须处于 active&quot;">​</a></h3><p><code>PlannerServer</code> 继承 <code>nav2_util::LifecycleNode</code>。进程存在不等于服务可用，只有节点进入 <code>active</code>，两个 Action Server、<code>/plan</code> Publisher、Global Costmap、规划插件和 <code>/is_path_valid</code> Service 才真正工作。</p><p>检查状态：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lifecycle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span></span></code></pre></div><p>正常结果应为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>active [3]</span></span></code></pre></div><p>如果还是 <code>unconfigured</code> 或 <code>inactive</code>，先检查 Lifecycle Manager 的 <code>node_names</code> 是否包含 <code>planner_server</code>，以及 <code>autostart</code> 是否开启。</p><h3 id="_2-2-global-costmap-必须能够持续更新" tabindex="-1">2.2 Global Costmap 必须能够持续更新 <a class="header-anchor" href="#_2-2-global-costmap-必须能够持续更新" aria-label="Permalink to &quot;2.2 Global Costmap 必须能够持续更新&quot;">​</a></h3><p>Planner Server 自己创建名为 <code>global_costmap</code> 的 <code>Costmap2DROS</code>。它依赖静态地图、传感器层、机器人 Footprint 和 TF。</p><p>Action 回调在计算前调用 <code>waitForCostmap()</code>，以 <code>100 Hz</code> 轮询 <code>costmap_ros_-&gt;isCurrent()</code>。当前实现没有超时，所以地图或传感器时间戳异常时，请求可能一直停在等待阶段。</p><h3 id="_2-3-tf-必须能把请求位姿变到全局坐标系" tabindex="-1">2.3 TF 必须能把请求位姿变到全局坐标系 <a class="header-anchor" href="#_2-3-tf-必须能把请求位姿变到全局坐标系" aria-label="Permalink to &quot;2.3 TF 必须能把请求位姿变到全局坐标系&quot;">​</a></h3><p>当 <code>use_start=false</code> 时，Planner Server 需要从 Costmap 获取机器人位姿。随后起点和目标都会通过 <code>transformPoseToGlobalFrame()</code> 转到 Global Costmap 的 <code>global_frame</code>，本工程通常为 <code>map</code>。</p><p>常用检查：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tf2_ros</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tf2_echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> map</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> base_link</span></span></code></pre></div><p>如果目标使用 <code>odom</code>、<code>base_link</code> 或其他 Frame，还必须保证该 Frame 到 <code>map</code> 的变换在目标时间戳处可用。</p><h3 id="_2-4-至少一个规划插件必须成功配置" tabindex="-1">2.4 至少一个规划插件必须成功配置 <a class="header-anchor" href="#_2-4-至少一个规划插件必须成功配置" aria-label="Permalink to &quot;2.4 至少一个规划插件必须成功配置&quot;">​</a></h3><p><code>on_configure()</code> 按 <code>planner_plugins</code> 读取每个 <code>&lt;planner_id&gt;.plugin</code>，通过 pluginlib 创建实例，并调用插件的 <code>configure()</code>。</p><p>插件类型拼写错误、库未安装或插件配置抛出异常，都会让 Planner Server 配置失败，节点无法进入 <code>active</code>。</p><p><img src="`+t+`" alt="Lifecycle 与运行条件"></p><h2 id="三、如何正确配置规划插件" tabindex="-1">三、如何正确配置规划插件 <a class="header-anchor" href="#三、如何正确配置规划插件" aria-label="Permalink to &quot;三、如何正确配置规划插件&quot;">​</a></h2><h3 id="_3-1-planner-plugins-是实例清单" tabindex="-1">3.1 <code>planner_plugins</code> 是实例清单 <a class="header-anchor" href="#_3-1-planner-plugins-是实例清单" aria-label="Permalink to &quot;3.1 \`planner_plugins\` 是实例清单&quot;">​</a></h3><p>当前工程一次加载六个规划器实例：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">planner_server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  ros__parameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    expected_planner_frequency</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20.0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    use_sim_time</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    selected_planner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GridBasedAstar&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    planner_plugins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GridBased&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GridBasedAstar&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Smac2D&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SmacHybrid&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SmacLattice&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ThetaStar&quot;</span></span></code></pre></div><p>列表里的字符串是实例 ID，不是 C++ 类型。Action 的 <code>planner_id</code>、<code>selected_planner</code> 和插件参数前缀都使用这个 ID。</p><h3 id="_3-2-planner-id-plugin-决定实现类型" tabindex="-1">3.2 <code>&lt;planner_id&gt;.plugin</code> 决定实现类型 <a class="header-anchor" href="#_3-2-planner-id-plugin-决定实现类型" aria-label="Permalink to &quot;3.2 \`&lt;planner_id&gt;.plugin\` 决定实现类型&quot;">​</a></h3><p>同一种 C++ 插件可以创建多个配置实例。当前 <code>GridBased</code> 和 <code>GridBasedAstar</code> 都使用 NavFn，只靠 <code>use_astar</code> 区分搜索方式：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">GridBased</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  plugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nav2_navfn_planner/NavfnPlanner&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  tolerance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  use_astar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  allow_unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">GridBasedAstar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  plugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nav2_navfn_planner/NavfnPlanner&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  tolerance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  use_astar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  allow_unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span></code></pre></div><p>因此，“插件 ID”和“插件 Type”必须分开理解：ID 是一次配置实例的名字，Type 是 pluginlib 要创建的类。</p><h3 id="_3-3-六个实例分别适合什么场景" tabindex="-1">3.3 六个实例分别适合什么场景 <a class="header-anchor" href="#_3-3-六个实例分别适合什么场景" aria-label="Permalink to &quot;3.3 六个实例分别适合什么场景&quot;">​</a></h3><ul><li><code>GridBased</code>：NavFn Dijkstra，势场传播完整，适合做稳定基线。</li><li><code>GridBasedAstar</code>：NavFn A*，通常扩展更少，本工程当前默认强制选择它。</li><li><code>Smac2D</code>：二维代价感知 A*，可把 Costmap 代价更明显地写入路径选择。</li><li><code>SmacHybrid</code>：状态包含位置和航向，适合存在最小转弯半径的车辆。</li><li><code>SmacLattice</code>：使用预生成运动基元，适合把底盘可执行运动直接放入搜索空间。</li><li><code>ThetaStar</code>：允许任意角连接，适合减少传统栅格路径的折线感。</li></ul><p>算法选择没有脱离 Costmap 参数的“绝对最优”。相同插件在不同 Inflation、Footprint、未知空间策略和目标容差下，也会得到完全不同的路径。</p><h3 id="_3-4-selected-planner-与-planner-id-的优先级" tabindex="-1">3.4 <code>selected_planner</code> 与 <code>planner_id</code> 的优先级 <a class="header-anchor" href="#_3-4-selected-planner-与-planner-id-的优先级" aria-label="Permalink to &quot;3.4 \`selected_planner\` 与 \`planner_id\` 的优先级&quot;">​</a></h3><p>当前源码最终使用：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::string requested_planner_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  selected_planner_id_.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">empty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> planner_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> selected_planner_id_;</span></span></code></pre></div><p>所以：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>selected_planner 非空</span></span>
<span class="line"><span>  → 服务器强制使用 selected_planner</span></span>
<span class="line"><span></span></span>
<span class="line"><span>selected_planner 为空</span></span>
<span class="line"><span>  → 每个 Action Goal 的 planner_id 生效</span></span></code></pre></div><p>现用配置是：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">selected_planner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GridBasedAstar&quot;</span></span></code></pre></div><p>即使客户端请求 <code>SmacHybrid</code>，服务器仍会使用 <code>GridBasedAstar</code>。要做多算法 A／B 对比，应先把 <code>selected_planner</code> 设为空，再分别发送不同 <code>planner_id</code>。</p><h2 id="四、推荐启动方式与接口检查" tabindex="-1">四、推荐启动方式与接口检查 <a class="header-anchor" href="#四、推荐启动方式与接口检查" aria-label="Permalink to &quot;四、推荐启动方式与接口检查&quot;">​</a></h2><h3 id="_4-1-通过完整-bringup-启动" tabindex="-1">4.1 通过完整 Bringup 启动 <a class="header-anchor" href="#_4-1-通过完整-bringup-启动" aria-label="Permalink to &quot;4.1 通过完整 Bringup 启动&quot;">​</a></h3><p>Planner Server 强依赖地图、TF、Costmap 和 Lifecycle。最稳妥的方式是通过已经配置好的 Bringup 启动，而不是只运行一个裸进程。</p><p>当前工程可使用：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/nav2_demo/nav2_ws/install/setup.bash</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> launch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nav2_regulated_modules</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> regulated_modules.launch.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  use_rviz:=False</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  use_sim_time:=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div><p>如果使用外部地图和参数：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> launch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nav2_regulated_modules</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> regulated_modules.launch.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  map:=/absolute/path/to/map.yaml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  params_file:=/absolute/path/to/regulated_modules.yaml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  use_rviz:=False</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  use_sim_time:=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div><h3 id="_4-2-裸进程为什么经常-启动了却不能规划" tabindex="-1">4.2 裸进程为什么经常“启动了却不能规划” <a class="header-anchor" href="#_4-2-裸进程为什么经常-启动了却不能规划" aria-label="Permalink to &quot;4.2 裸进程为什么经常“启动了却不能规划”&quot;">​</a></h3><p>下面的命令只创建 Lifecycle 节点进程：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nav2_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> planner_server</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --ros-args</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --params-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /absolute/path/to/nav2_params.yaml</span></span></code></pre></div><p>它还需要完整的 <code>global_costmap</code> 参数、地图输入、TF 和 Lifecycle 切换。若没有 Lifecycle Manager，可手动执行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lifecycle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> configure</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lifecycle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> activate</span></span></code></pre></div><p>手动激活只改变状态，不会替你补齐缺失的地图、传感器或 TF。</p><h3 id="_4-3-启动后检查哪些接口" tabindex="-1">4.3 启动后检查哪些接口 <a class="header-anchor" href="#_4-3-启动后检查哪些接口" aria-label="Permalink to &quot;4.3 启动后检查哪些接口&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> action</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compute_path</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> service</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> is_path_valid</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> info</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /plan</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> planner_plugins</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><p>也可以直接核对消息定义：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> interface</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nav2_msgs/action/ComputePathToPose</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> interface</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nav2_msgs/action/ComputePathThroughPoses</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> interface</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nav2_msgs/srv/IsPathValid</span></span></code></pre></div><h2 id="五、单目标规划如何调用" tabindex="-1">五、单目标规划如何调用 <a class="header-anchor" href="#五、单目标规划如何调用" aria-label="Permalink to &quot;五、单目标规划如何调用&quot;">​</a></h2><h3 id="_5-1-让服务器自动使用机器人当前位置" tabindex="-1">5.1 让服务器自动使用机器人当前位置 <a class="header-anchor" href="#_5-1-让服务器自动使用机器人当前位置" aria-label="Permalink to &quot;5.1 让服务器自动使用机器人当前位置&quot;">​</a></h3><p><code>use_start=false</code> 时，请求中的 <code>start</code> 被忽略，Planner Server 通过 Global Costmap 获取当前机器人位姿。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> action</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> send_goal</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  /compute_path_to_pose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  nav2_msgs/action/ComputePathToPose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    goal: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      header: {frame_id: map},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      pose: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        position: {x: 2.0, y: 1.0, z: 0.0},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    planner_id: GridBasedAstar,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    use_start: false</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  }&quot;</span></span></code></pre></div><p>这种模式适合真实导航，因为路径从机器人当前实际位置开始。</p><h3 id="_5-2-使用显式起点做离线规划" tabindex="-1">5.2 使用显式起点做离线规划 <a class="header-anchor" href="#_5-2-使用显式起点做离线规划" aria-label="Permalink to &quot;5.2 使用显式起点做离线规划&quot;">​</a></h3><p><code>use_start=true</code> 时，服务器使用 Goal 内的 <code>start</code>。这适合地图选点、算法对比和不依赖机器人当前位置的路径查询。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> action</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> send_goal</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  /compute_path_to_pose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  nav2_msgs/action/ComputePathToPose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    start: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      header: {frame_id: map},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      pose: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        position: {x: 0.0, y: 0.0, z: 0.0},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    goal: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      header: {frame_id: map},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      pose: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        position: {x: 2.0, y: 1.0, z: 0.0},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    planner_id: Smac2D,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    use_start: true</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  }&quot;</span></span></code></pre></div><p>若 <code>selected_planner</code> 仍为 <code>GridBasedAstar</code>，这里填写的 <code>Smac2D</code> 不会真正生效。</p><h3 id="_5-3-result-里有什么" tabindex="-1">5.3 Result 里有什么 <a class="header-anchor" href="#_5-3-result-里有什么" aria-label="Permalink to &quot;5.3 Result 里有什么&quot;">​</a></h3><p><code>ComputePathToPose</code> 没有 Feedback 字段。成功 Result 只包含：</p><ul><li><code>path</code>：插件返回的 <code>nav_msgs/Path</code>。</li><li><code>planning_time</code>：从 Action 回调开始到结果收口的总耗时。</li></ul><p>这个耗时包含等待 Costmap、获取位姿、TF 转换和插件计算，不是纯搜索算法耗时。</p><h2 id="六、多目标规划如何调用" tabindex="-1">六、多目标规划如何调用 <a class="header-anchor" href="#六、多目标规划如何调用" aria-label="Permalink to &quot;六、多目标规划如何调用&quot;">​</a></h2><h3 id="_6-1-一次发送多个途经点" tabindex="-1">6.1 一次发送多个途经点 <a class="header-anchor" href="#_6-1-一次发送多个途经点" aria-label="Permalink to &quot;6.1 一次发送多个途经点&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> action</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> send_goal</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  /compute_path_through_poses</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  nav2_msgs/action/ComputePathThroughPoses</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    goals: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        header: {frame_id: map},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        pose: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          position: {x: 1.0, y: 0.5, z: 0.0},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        header: {frame_id: map},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        pose: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          position: {x: 2.0, y: 1.0, z: 0.0},</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    ],</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    planner_id: GridBasedAstar,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    use_start: false</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  }&quot;</span></span></code></pre></div><h3 id="_6-2-多目标不是一次联合搜索" tabindex="-1">6.2 多目标不是一次联合搜索 <a class="header-anchor" href="#_6-2-多目标不是一次联合搜索" aria-label="Permalink to &quot;6.2 多目标不是一次联合搜索&quot;">​</a></h3><p>当前 <code>computePlanThroughPoses()</code> 会逐段调用同一个 <code>getPlan()</code>：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>机器人或显式起点</span></span>
<span class="line"><span>  → goals[0]</span></span>
<span class="line"><span>  → goals[1]</span></span>
<span class="line"><span>  → goals[2]</span></span>
<span class="line"><span>  → 拼接 Path</span></span></code></pre></div><p>后续段起点取上一段实际 Path 的最后 Pose，而不是机械地使用上一个请求目标。这样可以兼容规划插件的目标容差：插件即使停在目标附近，下一段也能从真实终点继续。</p><h3 id="_6-3-拼接结果的两个边界" tabindex="-1">6.3 拼接结果的两个边界 <a class="header-anchor" href="#_6-3-拼接结果的两个边界" aria-label="Permalink to &quot;6.3 拼接结果的两个边界&quot;">​</a></h3><p>各段 Path 的 <code>poses</code> 被直接追加，相邻分段的共同端点可能重复，Planner Server 不做去重。</p><p>另外，空 <code>goals</code> 在当前源码中虽然会调用 <code>terminate_current()</code>，但没有立即 <code>return</code>。客户端应在发送前保证数组非空，不要依赖服务器替空请求完成干净收口。</p><p><img src="`+p+'" alt="单目标与多目标 Action 数据流"></p><h2 id="七、planner-server-内部怎样完成一次规划" tabindex="-1">七、Planner Server 内部怎样完成一次规划 <a class="header-anchor" href="#七、planner-server-内部怎样完成一次规划" aria-label="Permalink to &quot;七、Planner Server 内部怎样完成一次规划&quot;">​</a></h2><h3 id="_7-1-action-状态与取消检查" tabindex="-1">7.1 Action 状态与取消检查 <a class="header-anchor" href="#_7-1-action-状态与取消检查" aria-label="Permalink to &quot;7.1 Action 状态与取消检查&quot;">​</a></h3><p>回调先确认 Action Server 已激活，再检查取消请求。取消被发现时调用 <code>terminate_all()</code>，结束当前及挂起 Goal。</p><h3 id="_7-2-等待-global-costmap-current" tabindex="-1">7.2 等待 Global Costmap current <a class="header-anchor" href="#_7-2-等待-global-costmap-current" aria-label="Permalink to &quot;7.2 等待 Global Costmap current&quot;">​</a></h3><p><code>waitForCostmap()</code> 会一直等待。当前循环没有超时、没有 <code>rclcpp::ok()</code> 条件，也不在循环内重新检查取消。</p><p>这意味着“Action 已发出但长时间没有结果”时，优先检查 Costmap 是否 current，而不是立刻怀疑规划算法。</p><h3 id="_7-3-取得起点并统一坐标系" tabindex="-1">7.3 取得起点并统一坐标系 <a class="header-anchor" href="#_7-3-取得起点并统一坐标系" aria-label="Permalink to &quot;7.3 取得起点并统一坐标系&quot;">​</a></h3><p>服务器根据 <code>use_start</code> 选择显式起点或机器人当前位姿，再把起点和目标都转换到 Global Costmap Frame。</p><p>任一 Pose 转换失败，Action 直接终止，规划插件不会被调用。</p><h3 id="_7-4-选择插件并调用-createplan" tabindex="-1">7.4 选择插件并调用 <code>createPlan()</code> <a class="header-anchor" href="#_7-4-选择插件并调用-createplan" aria-label="Permalink to &quot;7.4 选择插件并调用 `createPlan()`&quot;">​</a></h3><p><code>getPlan()</code> 先计算最终 Planner ID，再在 <code>planners_</code> 中查找实例，最后调用包外插件：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> planners_[requested_planner_id]-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createPlan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(start, goal);</span></span></code></pre></div><p>插件抛出的标准异常会被 Action 回调捕获并终止当前 Goal。</p><h3 id="_7-5-验证、发布并返回" tabindex="-1">7.5 验证、发布并返回 <a class="header-anchor" href="#_7-5-验证、发布并返回" aria-label="Permalink to &quot;7.5 验证、发布并返回&quot;">​</a></h3><p><code>validatePath()</code> 只检查 <code>path.poses</code> 是否为空。非空后，服务器可选发布 <code>/plan</code>，填写 <code>planning_time</code>，再把 Action 标记为成功。</p><p>因此，“Action 成功”只说明插件给出了非空 Path，不代表服务器已经逐点做过完整碰撞检查。</p><p><img src="'+o+'" alt="两个规划 Action 共用的守卫与输入整形函数"></p><h2 id="八、planner-id-的回退逻辑" tabindex="-1">八、Planner ID 的回退逻辑 <a class="header-anchor" href="#八、planner-id-的回退逻辑" aria-label="Permalink to &quot;八、Planner ID 的回退逻辑&quot;">​</a></h2><h3 id="_8-1-多插件场景" tabindex="-1">8.1 多插件场景 <a class="header-anchor" href="#_8-1-多插件场景" aria-label="Permalink to &quot;8.1 多插件场景&quot;">​</a></h3><p>加载多个插件时，最终 Planner ID 必须命中 <code>planners_</code>。无效 ID 会记录错误，返回空 Path，随后 Action 因 <code>validatePath()</code> 失败而终止。</p><h3 id="_8-2-单插件场景" tabindex="-1">8.2 单插件场景 <a class="header-anchor" href="#_8-2-单插件场景" aria-label="Permalink to &quot;8.2 单插件场景&quot;">​</a></h3><p>只加载一个插件时，空 ID 会回退到唯一插件；错误 ID 也会回退到唯一插件，并输出一次警告。</p><p>这种兼容逻辑方便最小配置，却可能掩盖客户端拼写错误。生产系统即使只有一个插件，也建议统一使用明确、可校验的 ID。</p><h3 id="_8-3-如何确认真正生效的-planner" tabindex="-1">8.3 如何确认真正生效的 Planner <a class="header-anchor" href="#_8-3-如何确认真正生效的-planner" aria-label="Permalink to &quot;8.3 如何确认真正生效的 Planner&quot;">​</a></h3><p>同时检查服务器强制值和请求值：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> planner_plugins</span></span></code></pre></div><p>若 <code>selected_planner</code> 非空，以它为准。若为空，再检查 Action Goal 的 <code>planner_id</code>。</p><p>日志中的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Action requested planner ..., but selected_planner is ...</span></span></code></pre></div><p>表示请求值已被服务器配置覆盖。</p><h2 id="九、-plan-topic-应该怎样理解" tabindex="-1">九、<code>/plan</code> Topic 应该怎样理解 <a class="header-anchor" href="#九、-plan-topic-应该怎样理解" aria-label="Permalink to &quot;九、`/plan` Topic 应该怎样理解&quot;">​</a></h2><h3 id="_9-1-它是观测输出-不是规划接口" tabindex="-1">9.1 它是观测输出，不是规划接口 <a class="header-anchor" href="#_9-1-它是观测输出-不是规划接口" aria-label="Permalink to &quot;9.1 它是观测输出，不是规划接口&quot;">​</a></h3><p><code>publishPlan()</code> 只有在 Lifecycle Publisher 已激活且至少存在一个订阅者时才真正发布。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /plan</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --once</span></span></code></pre></div><p>没有订阅者不会影响 Action Result，发布失败也不是规划失败的判据。上层导航应该读取 Action Result，而不是依赖 <code>/plan</code> 接收路径。</p><h3 id="_9-2-它不是固定频率-topic" tabindex="-1">9.2 它不是固定频率 Topic <a class="header-anchor" href="#_9-2-它不是固定频率-topic" aria-label="Permalink to &quot;9.2 它不是固定频率 Topic&quot;">​</a></h3><p>Planner Server 不会按 <code>expected_planner_frequency</code> 周期发布路径。只有外部调度器再次发送规划 Action，才会产生新路径。</p><p>因此：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hz</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /plan</span></span></code></pre></div><p>看到低频或无连续频率并不一定异常，它只反映外部重规划请求的节奏。</p><h2 id="十、如何使用-is-path-valid" tabindex="-1">十、如何使用 <code>/is_path_valid</code> <a class="header-anchor" href="#十、如何使用-is-path-valid" aria-label="Permalink to &quot;十、如何使用 `/is_path_valid`&quot;">​</a></h2><h3 id="_10-1-service-的输入输出" tabindex="-1">10.1 Service 的输入输出 <a class="header-anchor" href="#_10-1-service-的输入输出" aria-label="Permalink to &quot;10.1 Service 的输入输出&quot;">​</a></h3><p>请求是完整 <code>nav_msgs/Path</code>，响应包含：</p><ul><li><code>is_valid</code>：路径剩余部分是否避开当前致命障碍。</li><li><code>invalid_pose_indices</code>：接口预留的无效点索引。</li></ul><p>查看接口：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> interface</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nav2_msgs/srv/IsPathValid</span></span></code></pre></div><h3 id="_10-2-检查从机器人最近点开始" tabindex="-1">10.2 检查从机器人最近点开始 <a class="header-anchor" href="#_10-2-检查从机器人最近点开始" aria-label="Permalink to &quot;10.2 检查从机器人最近点开始&quot;">​</a></h3><p>服务先取得机器人当前 Pose，寻找整条 Path 中欧氏距离最近的点，只检查该点到终点。已经驶过的路径即使后来被障碍占据，也不会让剩余路径被判无效。</p><p>圆形机器人直接读取路径点所在 Costmap Cell；非圆形 Footprint 调用 <code>FootprintCollisionChecker</code>，按每个 Pose 的位置和航向检查占用。</p><p><img src="'+h+`" alt="路径有效性检查"></p><h3 id="_10-3-当前实现的使用边界" tabindex="-1">10.3 当前实现的使用边界 <a class="header-anchor" href="#_10-3-当前实现的使用边界" aria-label="Permalink to &quot;10.3 当前实现的使用边界&quot;">​</a></h3><p>当前代码不会填充 <code>invalid_pose_indices</code>，调用方只能可靠使用 <code>is_valid</code>。</p><p>服务假定 Path Pose 已经处于 Costmap 世界坐标系，不会逐点做 TF 转换。传入其他 Frame 的路径可能得到错误结论。</p><p>如果获取机器人 Pose 失败，<code>response-&gt;is_valid</code> 会保留初始化的 <code>true</code>。安全关键调用方应先独立确认定位和 TF 正常，不能把一次 <code>true</code> 直接当成完整安全证明。</p><h2 id="十一、运行时怎样切换-planner" tabindex="-1">十一、运行时怎样切换 Planner <a class="header-anchor" href="#十一、运行时怎样切换-planner" aria-label="Permalink to &quot;十一、运行时怎样切换 Planner&quot;">​</a></h2><h3 id="_11-1-在已加载插件之间切换" tabindex="-1">11.1 在已加载插件之间切换 <a class="header-anchor" href="#_11-1-在已加载插件之间切换" aria-label="Permalink to &quot;11.1 在已加载插件之间切换&quot;">​</a></h3><p>节点处于 <code>active</code> 时，可以修改：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Smac2D</span></span></code></pre></div><p>成功后，后续单目标和多目标请求都会优先使用 <code>Smac2D</code>。</p><p>恢复由 Action Goal 自主选择：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&#39;&#39;&quot;</span></span></code></pre></div><p>再核对：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><h3 id="_11-2-修改耗时告警阈值" tabindex="-1">11.2 修改耗时告警阈值 <a class="header-anchor" href="#_11-2-修改耗时告警阈值" aria-label="Permalink to &quot;11.2 修改耗时告警阈值&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> expected_planner_frequency</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5.0</span></span></code></pre></div><p>服务器把它换算成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>max_planner_duration = 1 / 5.0 = 0.2 s</span></span></code></pre></div><p>单次 Action 总耗时超过 <code>0.2 s</code> 时输出告警。设为零或负值会关闭这类告警，但参数更新仍返回成功。</p><h3 id="_11-3-哪些配置不能真正热加载" tabindex="-1">11.3 哪些配置不能真正热加载 <a class="header-anchor" href="#_11-3-哪些配置不能真正热加载" aria-label="Permalink to &quot;11.3 哪些配置不能真正热加载&quot;">​</a></h3><p><code>planner_plugins</code> 和 <code>&lt;planner_id&gt;.plugin</code> 只在 <code>on_configure()</code> 中用于创建 <code>planners_</code>。运行期修改参数存储，不会创建、配置或激活新插件。</p><p>要增加、删除或更换插件 Type，需要让节点完成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>deactivate</span></span>
<span class="line"><span>  → cleanup</span></span>
<span class="line"><span>  → 修改参数</span></span>
<span class="line"><span>  → configure</span></span>
<span class="line"><span>  → activate</span></span></code></pre></div><p>仅执行 deactivate／activate 不会重建插件表。</p><h3 id="_11-4-动态参数为什么可能等待很久" tabindex="-1">11.4 动态参数为什么可能等待很久 <a class="header-anchor" href="#_11-4-动态参数为什么可能等待很久" aria-label="Permalink to &quot;11.4 动态参数为什么可能等待很久&quot;">​</a></h3><p>两个规划 Action 和动态参数回调共用 <code>dynamic_params_lock_</code>，而且 Action 在完整规划流程期间一直持锁。</p><p>慢规划正在运行时，<code>ros2 param set</code> 会等待 Action 释放锁。参数命令延迟不一定是参数服务故障，也可能是当前规划仍占用互斥锁。</p><p><img src="`+r+'" alt="插件选择、动态参数与规划回调的共享状态"></p><h2 id="十二、最实用的排障路径" tabindex="-1">十二、最实用的排障路径 <a class="header-anchor" href="#十二、最实用的排障路径" aria-label="Permalink to &quot;十二、最实用的排障路径&quot;">​</a></h2><h3 id="_12-1-action-根本不存在" tabindex="-1">12.1 Action 根本不存在 <a class="header-anchor" href="#_12-1-action-根本不存在" aria-label="Permalink to &quot;12.1 Action 根本不存在&quot;">​</a></h3><p>检查节点和 Lifecycle：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> planner_server</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lifecycle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> action</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compute_path</span></span></code></pre></div><p>节点不存在时检查 Launch；节点存在但不 active 时检查 Lifecycle Manager、插件加载错误和 Global Costmap 配置。</p><h3 id="_12-2-action-已接受但一直不返回" tabindex="-1">12.2 Action 已接受但一直不返回 <a class="header-anchor" href="#_12-2-action-已接受但一直不返回" aria-label="Permalink to &quot;12.2 Action 已接受但一直不返回&quot;">​</a></h3><p>重点检查 Global Costmap 数据与时间：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /map</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --once</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /global_costmap/costmap</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --once</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tf2_ros</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tf2_echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> map</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> base_link</span></span></code></pre></div><p>当前 <code>waitForCostmap()</code> 无超时，Costmap 不 current 会让请求一直等待。</p><h3 id="_12-3-日志提示无法转换起点或目标" tabindex="-1">12.3 日志提示无法转换起点或目标 <a class="header-anchor" href="#_12-3-日志提示无法转换起点或目标" aria-label="Permalink to &quot;12.3 日志提示无法转换起点或目标&quot;">​</a></h3><p>检查请求 <code>header.frame_id</code>、时间戳和 TF 链。用于静态测试时可以省略时间戳，让消息使用默认零时间；真实系统应保证传感器、定位和节点时间源一致。</p><h3 id="_12-4-日志提示-planner-id-无效" tabindex="-1">12.4 日志提示 Planner ID 无效 <a class="header-anchor" href="#_12-4-日志提示-planner-id-无效" aria-label="Permalink to &quot;12.4 日志提示 Planner ID 无效&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> planner_plugins</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><p>确认 ID 大小写完全一致，并判断是否被非空 <code>selected_planner</code> 覆盖。修改插件清单后必须重新 configure，不能只热改参数。</p><h3 id="_12-5-action-返回失败或空-path" tabindex="-1">12.5 Action 返回失败或空 Path <a class="header-anchor" href="#_12-5-action-返回失败或空-path" aria-label="Permalink to &quot;12.5 Action 返回失败或空 Path&quot;">​</a></h3><p>先区分“插件没有被调用”和“插件返回空路径”：</p><ul><li>TF 失败、起点获取失败、Server inactive 时，插件不会执行。</li><li>Planner ID 无效或插件找不到可行路时，<code>getPlan()</code> 返回空 Path。</li><li>插件抛异常时，Action 回调捕获异常并终止。</li></ul><p>随后检查目标是否落在地图外、障碍内部或未知区域，机器人 Footprint 是否过大，Inflation 是否封死通道，以及插件的 <code>tolerance</code>、<code>allow_unknown</code>、最大搜索时间和运动学约束。</p><h3 id="_12-6-plan-没消息但-action-成功" tabindex="-1">12.6 <code>/plan</code> 没消息但 Action 成功 <a class="header-anchor" href="#_12-6-plan-没消息但-action-成功" aria-label="Permalink to &quot;12.6 `/plan` 没消息但 Action 成功&quot;">​</a></h3><p>先启动订阅者再发 Goal：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /plan</span></span></code></pre></div><p>当前 <code>publishPlan()</code> 在没有订阅者时不会发布；真正可靠的路径仍在 Action Result 中。</p><h2 id="十三、源码阅读后必须知道的实现边界" tabindex="-1">十三、源码阅读后必须知道的实现边界 <a class="header-anchor" href="#十三、源码阅读后必须知道的实现边界" aria-label="Permalink to &quot;十三、源码阅读后必须知道的实现边界&quot;">​</a></h2><h3 id="_13-1-抢占-goal-没有回写调用者" tabindex="-1">13.1 抢占 Goal 没有回写调用者 <a class="header-anchor" href="#_13-1-抢占-goal-没有回写调用者" aria-label="Permalink to &quot;13.1 抢占 Goal 没有回写调用者&quot;">​</a></h3><p><code>getPreemptedGoalIfRequested()</code> 的 Goal shared pointer 按值传入。函数内部接受 pending Goal 后只修改本地副本，不会更新 Action 回调里的 <code>goal</code> 变量。当前抢占行为不能按接口名字想当然理解。</p><h3 id="_13-2-costmap-等待缺少退出条件" tabindex="-1">13.2 Costmap 等待缺少退出条件 <a class="header-anchor" href="#_13-2-costmap-等待缺少退出条件" aria-label="Permalink to &quot;13.2 Costmap 等待缺少退出条件&quot;">​</a></h3><p>等待循环没有超时、取消复查或 <code>rclcpp::ok()</code> 条件，而且发生在完整规划锁内。Costmap 长期不 current 时，规划和动态参数更新都可能被拖住。</p><h3 id="_13-3-validatepath-只判断非空" tabindex="-1">13.3 <code>validatePath()</code> 只判断非空 <a class="header-anchor" href="#_13-3-validatepath-只判断非空" aria-label="Permalink to &quot;13.3 `validatePath()` 只判断非空&quot;">​</a></h3><p>函数名容易让人误以为它会做碰撞检查，但它只判断 <code>path.poses.size()</code> 是否为零。路径碰撞检查要显式调用 <code>/is_path_valid</code>，或依赖具体规划插件的搜索约束。</p><h3 id="_13-4-多目标空数组终止后仍继续" tabindex="-1">13.4 多目标空数组终止后仍继续 <a class="header-anchor" href="#_13-4-多目标空数组终止后仍继续" aria-label="Permalink to &quot;13.4 多目标空数组终止后仍继续&quot;">​</a></h3><p><code>goals.empty()</code> 分支调用 <code>terminate_current()</code> 后没有立即返回。客户端应在输入侧拒绝空数组，服务端源码也应补充明确的 <code>return</code>。</p><h3 id="_13-5-路径有效性失败可能被误判为有效" tabindex="-1">13.5 路径有效性失败可能被误判为有效 <a class="header-anchor" href="#_13-5-路径有效性失败可能被误判为有效" aria-label="Permalink to &quot;13.5 路径有效性失败可能被误判为有效&quot;">​</a></h3><p><code>isPathValid()</code> 获取机器人 Pose 失败时不会把结果改为 <code>false</code>；<code>invalid_pose_indices</code> 也没有填充。安全流程需要把定位健康、TF 可用和路径检查三者联合判断。</p><h3 id="_13-6-动态参数批量更新不具备成员回滚" tabindex="-1">13.6 动态参数批量更新不具备成员回滚 <a class="header-anchor" href="#_13-6-动态参数批量更新不具备成员回滚" aria-label="Permalink to &quot;13.6 动态参数批量更新不具备成员回滚&quot;">​</a></h3><p>回调在遍历参数时直接修改 C++ 成员。如果同一批原子参数中的后续项被拒绝，ROS 参数存储不会提交，但此前成员副作用不会自动回滚。</p><p>运行时建议一次只改一个 Server 参数；源码层更稳妥的实现是先校验整批参数，再统一写入成员。</p><h2 id="十四、21-个生产函数逐个拆解" tabindex="-1">十四、21 个生产函数逐个拆解 <a class="header-anchor" href="#十四、21-个生产函数逐个拆解" aria-label="Permalink to &quot;十四、21 个生产函数逐个拆解&quot;">​</a></h2><h3 id="_14-1-main-把-planner-server-作为独立进程运行" tabindex="-1">14.1 <code>main()</code>：把 Planner Server 作为独立进程运行 <a class="header-anchor" href="#_14-1-main-把-planner-server-作为独立进程运行" aria-label="Permalink to &quot;14.1 `main()`：把 Planner Server 作为独立进程运行&quot;">​</a></h3><ul><li>作用：完成 ROS 2 上下文初始化、日志系统初始化、节点构造、Executor 阻塞运行和退出清理，是独立可执行文件 <code>planner_server</code> 的进程入口。</li><li>输入：操作系统传入的 <code>argc</code> 与 <code>argv</code>，其中可能包含 ROS 参数文件、命名空间、节点重命名和日志级别等 ROS 参数。</li><li>输出：正常结束时返回整数 <code>0</code>；运行期间产生一个 <code>PlannerServer</code> 节点，并把节点的 base interface 交给 <code>rclcpp::spin()</code>。</li><li>数据流与回调：函数本身不处理规划请求。<code>spin()</code> 持续驱动 Lifecycle、Action、Service 和参数回调，直到 ROS 上下文关闭。</li><li>包外调用：<code>rclcpp</code> 负责进程级 ROS 初始化与事件循环；<code>SpdlogWrapper</code> 在 ROS 节点创建前建立文件日志，在 ROS 关闭后释放日志资源。</li></ul><h3 id="_14-2-plannerserver-声明参数并搭起资源外壳" tabindex="-1">14.2 <code>PlannerServer()</code>：声明参数并搭起资源外壳 <a class="header-anchor" href="#_14-2-plannerserver-声明参数并搭起资源外壳" aria-label="Permalink to &quot;14.2 `PlannerServer()`：声明参数并搭起资源外壳&quot;">​</a></h3><ul><li>作用：构造名为 <code>planner_server</code> 的 Lifecycle 节点，准备 GlobalPlanner 插件加载器，声明规划器清单、强制选择和耗时告警参数，并创建名为 <code>global_costmap</code> 的 Costmap 节点外壳。</li><li>输入：<code>rclcpp::NodeOptions</code>，允许组件容器或独立进程传入参数覆盖、命名空间和进程内通信选项。</li><li>输出：构造完成的 <code>PlannerServer</code> 对象；成员中已有 <code>gp_loader_</code>、<code>planner_ids_</code> 和 <code>costmap_ros_</code>，但插件、Action、Publisher 与 Costmap 数据还没有进入可用状态。</li><li>数据流与回调：这是对象创建入口，不是 Lifecycle 回调。真正的资源初始化被推迟到 <code>on_configure()</code>，因此“进程存在”不等于“能够规划”。</li><li>包外调用：基类 <code>nav2_util::LifecycleNode</code> 提供 Lifecycle 能力；<code>pluginlib::ClassLoader</code> 只准备插件工厂；<code>nav2_costmap_2d::Costmap2DROS</code> 创建独立的 Global Costmap 生命周期节点。</li></ul><h3 id="_14-3-plannerserver-为异常退出保留最后一道清理" tabindex="-1">14.3 <code>~PlannerServer()</code>：为异常退出保留最后一道清理 <a class="header-anchor" href="#_14-3-plannerserver-为异常退出保留最后一道清理" aria-label="Permalink to &quot;14.3 `~PlannerServer()`：为异常退出保留最后一道清理&quot;">​</a></h3><ul><li>作用：清空插件映射并停止 Costmap 专用线程，防止正常 <code>deactivate</code>／<code>cleanup</code> 流程没有执行时留下后台执行资源。</li><li>输入：没有显式参数，输入是对象析构时仍然存活的成员状态。</li><li>输出：没有返回值；副作用是销毁 <code>planners_</code> 中的插件智能指针，并释放 <code>costmap_thread_</code>。</li><li>数据流与回调：它不是 Lifecycle 回调，而是 C++ 对象生命期的兜底。正常停机仍应先走 <code>on_deactivate()</code> 与 <code>on_cleanup()</code>，让插件按协议完成各自清理。</li><li>包外调用：插件对象和 <code>nav2_util::NodeThread</code> 都通过 RAII 析构释放，本函数不再主动调用包外业务接口。</li></ul><h3 id="_14-4-on-configure-一次性建立全部规划能力" tabindex="-1">14.4 <code>on_configure()</code>：一次性建立全部规划能力 <a class="header-anchor" href="#_14-4-on-configure-一次性建立全部规划能力" aria-label="Permalink to &quot;14.4 `on_configure()`：一次性建立全部规划能力&quot;">​</a></h3><ul><li>作用：配置 Global Costmap，取得 Costmap 裸指针和 TF Buffer，按 <code>planner_plugins</code> 创建并配置每个 GlobalPlanner 插件，创建 <code>/plan</code> Publisher 和两个规划 Action Server。</li><li>输入：Lifecycle 状态参数在当前实现中未使用；真实输入来自 ROS 参数、插件库、地图配置、TF 配置和 Costmap 配置。</li><li>输出：成功时返回 <code>CallbackReturn::SUCCESS</code>；插件实例化异常时返回 <code>FAILURE</code>。成功副作用包括写入 <code>planners_</code>、<code>planner_types_</code>、<code>planner_ids_concat_</code>、<code>max_planner_duration_</code>、<code>selected_planner_id_</code> 与多个接口句柄。</li><li>数据流与回调：Lifecycle Manager 触发该回调。函数先配置 Costmap，再启动 Costmap 的 <code>NodeThread</code>，随后把同一份 TF 与 Costmap 句柄交给每个规划插件，最后创建 Action 与 Publisher。</li><li>包外调用：Costmap 包提供地图、机器人模型和 TF；<code>nav2_util::get_plugin_type_param()</code>读取 <code>&lt;ID&gt;.plugin</code>；pluginlib 创建动态库实例；每个 <code>nav2_core::GlobalPlanner</code> 插件接收节点、实例 ID、TF 和 Costmap。</li></ul><h3 id="_14-5-on-activate-把已配置资源切换为可服务状态" tabindex="-1">14.5 <code>on_activate()</code>：把已配置资源切换为可服务状态 <a class="header-anchor" href="#_14-5-on-activate-把已配置资源切换为可服务状态" aria-label="Permalink to &quot;14.5 `on_activate()`：把已配置资源切换为可服务状态&quot;">​</a></h3><ul><li>作用：激活 Path Publisher、两个 Action Server、Global Costmap 和所有规划插件，并创建 <code>/is_path_valid</code> Service、动态参数回调与 Lifecycle Bond。</li><li>输入：Lifecycle 激活事件，以及 <code>on_configure()</code> 已经建立的全部资源。</li><li>输出：返回 <code>CallbackReturn::SUCCESS</code>；副作用是对外接口真正可用、参数修改开始被 Server 回调拦截、Lifecycle Manager 能通过 Bond 监控节点存活。</li><li>数据流与回调：Lifecycle Manager 触发激活。激活之后，规划 Goal 才会进入 <code>computePlan()</code> 或 <code>computePlanThroughPoses()</code>，参数提交才会进入 <code>dynamicParametersCallback()</code>。</li><li>包外调用：Lifecycle Publisher 与 SimpleActionServer 完成激活；Costmap 和每个规划插件接收各自的 <code>activate()</code>；ROS 2 参数系统注册提交前回调；<code>nav2_util::LifecycleNode</code> 创建 Bond。</li></ul><h3 id="_14-6-on-deactivate-停止规划入口但保留已配置资源" tabindex="-1">14.6 <code>on_deactivate()</code>：停止规划入口但保留已配置资源 <a class="header-anchor" href="#_14-6-on-deactivate-停止规划入口但保留已配置资源" aria-label="Permalink to &quot;14.6 `on_deactivate()`：停止规划入口但保留已配置资源&quot;">​</a></h3><ul><li>作用：停用两个规划 Action、Path Publisher、Global Costmap 和全部插件，注销动态参数回调并销毁 Bond。</li><li>输入：Lifecycle 停用事件，以及 active 状态中的资源句柄。</li><li>输出：返回 <code>CallbackReturn::SUCCESS</code>；插件表、Action 对象和 Publisher 对象仍然保留，可供后续再次激活。</li><li>数据流与回调：Lifecycle Manager 触发停用。函数切断新规划请求和参数热更新，但不会执行插件 <code>cleanup()</code>，因此它对应“暂停服务”，不是“彻底释放”。</li><li>包外调用：SimpleActionServer、Lifecycle Publisher、Costmap、GlobalPlanner 插件和 Bond 分别执行停用协议。普通 ROS Service 不具备 Lifecycle 自动停用语义，当前代码也没有在这里释放 <code>is_path_valid_service_</code>。</li></ul><h3 id="_14-7-on-cleanup-拆掉-configure-阶段建立的资源" tabindex="-1">14.7 <code>on_cleanup()</code>：拆掉 configure 阶段建立的资源 <a class="header-anchor" href="#_14-7-on-cleanup-拆掉-configure-阶段建立的资源" aria-label="Permalink to &quot;14.7 `on_cleanup()`：拆掉 configure 阶段建立的资源&quot;">​</a></h3><ul><li>作用：释放两个 Action Server、Path Publisher、TF Buffer、插件表和 Costmap 线程，把 <code>costmap_</code> 裸指针清空，并让 Costmap 与插件执行清理。</li><li>输入：Lifecycle cleanup 事件，以及 configure 或 deactivate 状态留下的资源。</li><li>输出：返回 <code>CallbackReturn::SUCCESS</code>；节点回到未配置状态，下一次使用前必须重新执行 <code>on_configure()</code>。</li><li>数据流与回调：Lifecycle Manager 触发清理。资源释放顺序先清 Action、Publisher 和 TF 句柄，再清 Costmap 与插件，最后停止线程并清空裸指针。</li><li>包外调用：Costmap 与每个 GlobalPlanner 插件执行 <code>cleanup()</code>；<code>NodeThread</code> 析构停止 Costmap 的独立执行器。当前函数没有显式释放 <code>is_path_valid_service_</code>，这是需要额外注意的生命周期边界。</li></ul><h3 id="_14-8-on-shutdown-记录进入最终状态" tabindex="-1">14.8 <code>on_shutdown()</code>：记录进入最终状态 <a class="header-anchor" href="#_14-8-on-shutdown-记录进入最终状态" aria-label="Permalink to &quot;14.8 `on_shutdown()`：记录进入最终状态&quot;">​</a></h3><ul><li>作用：响应 Lifecycle shutdown 事件并记录日志。</li><li>输入：Lifecycle 状态对象，当前函数不读取其中字段。</li><li>输出：固定返回 <code>CallbackReturn::SUCCESS</code>，不直接释放插件、Costmap、Action 或 Service。</li><li>数据流与回调：它只标记 Lifecycle 的最终状态；实际资源释放仍依赖此前的停用／清理流程和 C++ 析构兜底。</li><li>包外调用：没有规划业务调用，只使用日志设施。</li></ul><h3 id="_14-9-isserverinactive-action-回调的入口守卫" tabindex="-1">14.9 <code>isServerInactive()</code>：Action 回调的入口守卫 <a class="header-anchor" href="#_14-9-isserverinactive-action-回调的入口守卫" aria-label="Permalink to &quot;14.9 `isServerInactive()`：Action 回调的入口守卫&quot;">​</a></h3><ul><li>作用：确认目标 Action Server 指针存在并处于 active，避免未配置、已停用或正在清理时继续执行规划。</li><li>输入：单目标或多目标 <code>SimpleActionServer</code> 智能指针的引用。</li><li>输出：返回布尔值；<code>true</code> 表示调用者应立即结束，<code>false</code> 表示可以继续。</li><li>数据流与回调：<code>computePlan()</code> 和 <code>computePlanThroughPoses()</code> 进入 <code>try</code> 后首先调用它，它只读 Action Server 状态，不改变 Goal。</li><li>包外调用：<code>nav2_util::SimpleActionServer::is_server_active()</code> 查询 Action 生命周期状态。</li></ul><h3 id="_14-10-waitforcostmap-在算法运行前等待地图新鲜" tabindex="-1">14.10 <code>waitForCostmap()</code>：在算法运行前等待地图新鲜 <a class="header-anchor" href="#_14-10-waitforcostmap-在算法运行前等待地图新鲜" aria-label="Permalink to &quot;14.10 `waitForCostmap()`：在算法运行前等待地图新鲜&quot;">​</a></h3><ul><li>作用：以 <code>100 Hz</code> 轮询 Global Costmap 的 <code>isCurrent()</code>，确保清图恢复或传感器更新后地图重新有效。</li><li>输入：没有显式参数；隐式输入是 <code>costmap_ros_</code> 的 current 状态。</li><li>输出：没有返回值。只有 Costmap 变为 current 后函数才结束，否则一直阻塞。</li><li>数据流与回调：两个规划 Action 在检查 active 与取消之后调用它。等待时间被计入 <code>planning_time</code>，并且等待期间仍持有 <code>dynamic_params_lock_</code>。</li><li>包外调用：Costmap 提供 <code>isCurrent()</code>；<code>rclcpp::Rate</code> 提供轮询节拍。当前循环没有超时、<code>rclcpp::ok()</code> 检查或再次检查取消请求。</li></ul><h3 id="_14-11-iscancelrequested-把客户端取消转换为-action-终止" tabindex="-1">14.11 <code>isCancelRequested()</code>：把客户端取消转换为 Action 终止 <a class="header-anchor" href="#_14-11-iscancelrequested-把客户端取消转换为-action-终止" aria-label="Permalink to &quot;14.11 `isCancelRequested()`：把客户端取消转换为 Action 终止&quot;">​</a></h3><ul><li>作用：检查规划 Goal 是否收到取消请求，收到后终止该 Action Server 管理的全部 Goal。</li><li>输入：单目标或多目标 Action Server 智能指针的引用。</li><li>输出：返回布尔值；取消时返回 <code>true</code>，并调用 <code>terminate_all()</code>；未取消时返回 <code>false</code>。</li><li>数据流与回调：两个规划 Action 只在进入等待 Costmap 之前调用一次。进入 <code>waitForCostmap()</code> 或插件 <code>createPlan()</code> 后，本函数不会被周期性复查。</li><li>包外调用：SimpleActionServer 提供取消状态和 Action 终止能力，ROS 2 Action 层负责把终止状态反馈给客户端。</li></ul><h3 id="_14-12-getpreemptedgoalifrequested-尝试接收抢占-goal" tabindex="-1">14.12 <code>getPreemptedGoalIfRequested()</code>：尝试接收抢占 Goal <a class="header-anchor" href="#_14-12-getpreemptedgoalifrequested-尝试接收抢占-goal" aria-label="Permalink to &quot;14.12 `getPreemptedGoalIfRequested()`：尝试接收抢占 Goal&quot;">​</a></h3><ul><li>作用：检查是否有 pending Goal；存在时调用 <code>accept_pending_goal()</code> 取得新 Goal。</li><li>输入：Action Server 引用和当前 Goal 的 <code>shared_ptr</code>。</li><li>输出：没有返回值；设计意图是让后续规划使用新 Goal。</li><li>数据流与回调：两个规划 Action 在 Costmap current 后调用它。当前 Goal 参数按值传入，函数内部替换的只是局部智能指针，调用者保存的 <code>goal</code> 不会改变，因此抢占 Goal 实际没有进入后续规划数据流。</li><li>包外调用：SimpleActionServer 管理 pending Goal 与抢占状态。本包之外的 Action 层能够接受新 Goal，但本函数的 C++ 传参方式切断了回写。</li></ul><h3 id="_14-13-getstartpose-在显式起点与机器人当前位置之间选择" tabindex="-1">14.13 <code>getStartPose()</code>：在显式起点与机器人当前位置之间选择 <a class="header-anchor" href="#_14-13-getstartpose-在显式起点与机器人当前位置之间选择" aria-label="Permalink to &quot;14.13 `getStartPose()`：在显式起点与机器人当前位置之间选择&quot;">​</a></h3><ul><li>作用：根据 Goal 的 <code>use_start</code> 决定规划起点；显式模式复制 <code>goal-&gt;start</code>，自动模式从 Global Costmap 获取机器人当前 Pose。</li><li>输入：Action Server、Action Goal 和用于承接结果的 <code>PoseStamped &amp; start</code>。</li><li>输出：成功时返回 <code>true</code> 并写入 <code>start</code>；自动取位姿失败时终止当前 Goal 并返回 <code>false</code>。</li><li>数据流与回调：它同时服务单目标和多目标 Action。输出起点还没有保证处于 global frame，随后必须交给 <code>transformPosesToGlobalFrame()</code>。</li><li>包外调用：Costmap 通过 TF 查询机器人位姿；SimpleActionServer 在失败时负责终止当前 Action。</li></ul><h3 id="_14-14-transformposestoglobalframe-把算法输入统一到-costmap-坐标系" tabindex="-1">14.14 <code>transformPosesToGlobalFrame()</code>：把算法输入统一到 Costmap 坐标系 <a class="header-anchor" href="#_14-14-transformposestoglobalframe-把算法输入统一到-costmap-坐标系" aria-label="Permalink to &quot;14.14 `transformPosesToGlobalFrame()`：把算法输入统一到 Costmap 坐标系&quot;">​</a></h3><ul><li>作用：分别把当前起点与当前目标原地转换到 Global Costmap 的 global frame。</li><li>输入：Action Server，以及可写的 <code>curr_start</code> 与 <code>curr_goal</code>。</li><li>输出：两次转换都成功时返回 <code>true</code>，两个 Pose 被原地改写；任一转换失败时终止当前 Goal 并返回 <code>false</code>。</li><li>数据流与回调：单目标 Action 调用一次；多目标 Action 对每一段调用一次。它是 Action 消息与 GlobalPlanner 插件之间的坐标系适配层。</li><li>包外调用：<code>Costmap2DROS::transformPoseToGlobalFrame()</code> 使用 TF Buffer 和消息时间戳完成转换；SimpleActionServer 负责失败状态。</li></ul><h3 id="_14-15-validatepath-只做最小的非空结果守卫" tabindex="-1">14.15 <code>validatePath()</code>：只做最小的非空结果守卫 <a class="header-anchor" href="#_14-15-validatepath-只做最小的非空结果守卫" aria-label="Permalink to &quot;14.15 `validatePath()`：只做最小的非空结果守卫&quot;">​</a></h3><ul><li>作用：判断插件返回的 <code>nav_msgs::Path</code> 是否至少包含一个 Pose。</li><li>输入：Action Server、当前目标、插件返回的 Path 和用于日志的 <code>planner_id</code>。</li><li>输出：Path 非空返回 <code>true</code>；Path 为空时记录目标坐标、终止当前 Goal 并返回 <code>false</code>。</li><li>数据流与回调：它位于 <code>getPlan()</code> 之后、<code>publishPlan()</code> 之前。单目标调用一次，多目标对每一段调用一次。</li><li>包外调用：只借助 SimpleActionServer 终止失败 Goal。它不会调用 Costmap 或碰撞检查器，因此函数名中的“validate”不代表完整路径安全验证。</li></ul><h3 id="_14-16-computeplanthroughposes-把多个目标拆成多次插件调用" tabindex="-1">14.16 <code>computePlanThroughPoses()</code>：把多个目标拆成多次插件调用 <a class="header-anchor" href="#_14-16-computeplanthroughposes-把多个目标拆成多次插件调用" aria-label="Permalink to &quot;14.16 `computePlanThroughPoses()`：把多个目标拆成多次插件调用&quot;">​</a></h3><ul><li>作用：实现 <code>/compute_path_through_poses</code> Action 回调，把 <code>goals[]</code> 拆成连续分段，对每段调用一次 <code>getPlan()</code>，再直接拼接全部 Pose。</li><li>输入：从 <code>action_server_poses_</code> 读取 <code>goals[]</code>、<code>start</code>、<code>use_start</code> 和 <code>planner_id</code>，同时读取 Costmap、TF、插件表与耗时阈值。</li><li>输出：成功时返回拼接后的 Path 与从回调入口开始计算的 <code>planning_time</code>，同时按条件发布 <code>/plan</code> 并把 Action 标为成功；失败时终止当前 Goal。</li><li>数据流与回调：函数全程持有 <code>dynamic_params_lock_</code>。首段使用请求起点或机器人 Pose，后续段使用上一段 Path 的真实末点；每段依次经过 TF、<code>getPlan()</code> 和非空校验。</li><li>包外调用：SimpleActionServer 提供 Goal、Result 与状态；Costmap／TF 提供环境和坐标；GlobalPlanner 插件真正搜索路径；ROS 时钟计算耗时。各段直接追加，交界 Pose 可能重复。</li></ul><h3 id="_14-17-computeplan-单目标规划的总编排回调" tabindex="-1">14.17 <code>computePlan()</code>：单目标规划的总编排回调 <a class="header-anchor" href="#_14-17-computeplan-单目标规划的总编排回调" aria-label="Permalink to &quot;14.17 `computePlan()`：单目标规划的总编排回调&quot;">​</a></h3><ul><li>作用：实现 <code>/compute_path_to_pose</code> Action 回调，把状态守卫、Costmap 等待、起点选择、TF、插件调用、Path 校验、发布、耗时和异常处理串成一次事务。</li><li>输入：从 <code>action_server_pose_</code> 读取 <code>goal</code>、<code>start</code>、<code>use_start</code> 与 <code>planner_id</code>，并读取共享的 Costmap、插件表、强制 Planner ID 和耗时阈值。</li><li>输出：成功时返回 <code>path</code> 与 <code>planning_time</code>，发布可视化 Path 并把 Goal 标记为成功；守卫或异常失败时终止 Goal。</li><li>数据流与回调：函数由 SimpleActionServer 的执行回调触发，全程持有 <code>dynamic_params_lock_</code>。<code>planning_time</code> 包含等待 Costmap、获取机器人 Pose、TF 转换和插件计算，不只是算法耗时。</li><li>包外调用：Action Server 管理 Goal 状态，Costmap 和 TF 准备输入，GlobalPlanner 生成路径，Lifecycle Publisher 提供观察旁路，ROS 时钟用于耗时统计。</li></ul><h3 id="_14-18-getplan-决定最终调用哪个规划插件" tabindex="-1">14.18 <code>getPlan()</code>：决定最终调用哪个规划插件 <a class="header-anchor" href="#_14-18-getplan-决定最终调用哪个规划插件" aria-label="Permalink to &quot;14.18 `getPlan()`：决定最终调用哪个规划插件&quot;">​</a></h3><ul><li>作用：在 <code>selected_planner_id_</code> 与 Action <code>planner_id</code> 之间确定最终实例，从 <code>planners_</code> 查找插件并调用 <code>createPlan(start, goal)</code>。</li><li>输入：已经转换到 global frame 的起点、目标和 Action 请求的 Planner ID。</li><li>输出：命中插件时返回该插件生成的 Path；多插件且 ID 无效时返回空 Path；只有一个插件时可对空 ID 或错误 ID自动回退到唯一实例。</li><li>数据流与回调：<code>selected_planner_id_</code> 非空时拥有最高优先级，会覆盖两个 Action 传入的 <code>planner_id</code>。返回的 Path 随后进入 <code>validatePath()</code>。</li><li>包外调用：<code>nav2_core::GlobalPlanner::createPlan()</code> 是本包与具体 NavFn、Smac 或 Theta* 算法的统一边界。算法内部怎样搜索不在本包展开，本函数只负责实例选择和调用。</li></ul><h3 id="_14-19-publishplan-把结果复制到可视化旁路" tabindex="-1">14.19 <code>publishPlan()</code>：把结果复制到可视化旁路 <a class="header-anchor" href="#_14-19-publishplan-把结果复制到可视化旁路" aria-label="Permalink to &quot;14.19 `publishPlan()`：把结果复制到可视化旁路&quot;">​</a></h3><ul><li>作用：把 Path 封装为独占消息，并在 Publisher 已激活且至少存在一个订阅者时发布 <code>/plan</code>。</li><li>输入：成功规划得到的 <code>nav_msgs::Path</code> 常量引用。</li><li>输出：没有返回值；可能产生一次 Topic 消息，也可能因为没有订阅者而不发送。</li><li>数据流与回调：单目标或多目标 Action 成功校验后调用。它不改变 Action Result，业务调用者仍应从对应 Action Result 获取 Path。</li><li>包外调用：ROS 2 Lifecycle Publisher 管理 active 状态、订阅计数和消息发布；RViz 或调试节点通常是订阅者。</li></ul><h3 id="_14-20-ispathvalid-检查机器人前方的剩余-path" tabindex="-1">14.20 <code>isPathValid()</code>：检查机器人前方的剩余 Path <a class="header-anchor" href="#_14-20-ispathvalid-检查机器人前方的剩余-path" aria-label="Permalink to &quot;14.20 `isPathValid()`：检查机器人前方的剩余 Path&quot;">​</a></h3><ul><li>作用：实现 <code>/is_path_valid</code> Service，先找机器人当前位置最近的 Path 点，再从该点向终点检查每个 Pose 是否与当前 Costmap 冲突。</li><li>输入：Request 中的完整 Path，以及共享的机器人 Pose、Costmap、圆形／多边形机器人模型和 Path Pose 航向。</li><li>输出：Response 的 <code>is_valid</code>；空 Path 或检测到致命冲突时为 <code>false</code>，全部通过时为 <code>true</code>。接口定义中的 <code>invalid_pose_indices</code> 当前没有填写。</li><li>数据流与回调：普通 ROS Service 请求直接触发。圆形模型读取 Pose 所在 Cell 的代价值，多边形模型调用 Footprint 碰撞检查；已经驶过的 Path 前缀不再检查。</li><li>包外调用：Costmap 提供机器人 Pose、地图互斥锁、世界坐标转栅格和 Cell cost；TF2 从四元数提取航向；<code>FootprintCollisionChecker</code> 计算多边形轮廓覆盖代价。当前没有逐点转换 Path frame，机器人 Pose 获取失败时响应会保留初始 <code>true</code>。</li></ul><h3 id="_14-21-dynamicparameterscallback-在参数提交前更新运行成员" tabindex="-1">14.21 <code>dynamicParametersCallback()</code>：在参数提交前更新运行成员 <a class="header-anchor" href="#_14-21-dynamicparameterscallback-在参数提交前更新运行成员" aria-label="Permalink to &quot;14.21 `dynamicParametersCallback()`：在参数提交前更新运行成员&quot;">​</a></h3><ul><li>作用：处理运行期参数批次，把 <code>expected_planner_frequency</code> 转换为单次规划耗时阈值，并校验／更新 <code>selected_planner</code>。</li><li>输入：ROS 2 参数系统提交的 <code>std::vector&lt;rclcpp::Parameter&gt;</code>，其中只识别目标名称和匹配类型。</li><li>输出：返回 <code>SetParametersResult</code>。Planner ID 合法或清空时返回成功；非空 ID 未出现在 <code>planners_</code> 时拒绝整批提交并给出原因。</li><li>数据流与回调：只在 active 阶段注册。它与两个规划 Action 共用 <code>dynamic_params_lock_</code>，因此参数更新不会与一次规划并发改写成员，但慢规划、Costmap 永久等待或插件阻塞也会拖住参数服务。</li><li>包外调用：ROS 2 参数系统把返回值当作提交前票决。插件表不会因为参数变化而重建；本函数只在 configure 阶段已加载的实例之间切换。批次遍历中先写成员、后遇到非法参数时，ROS 参数存储会拒绝提交，但已经发生的成员写入不会自动回滚。</li></ul><h2 id="十五、把-planner-server-用稳的配置原则" tabindex="-1">十五、把 Planner Server 用稳的配置原则 <a class="header-anchor" href="#十五、把-planner-server-用稳的配置原则" aria-label="Permalink to &quot;十五、把 Planner Server 用稳的配置原则&quot;">​</a></h2><h3 id="_15-1-先建立单插件基线" tabindex="-1">15.1 先建立单插件基线 <a class="header-anchor" href="#_15-1-先建立单插件基线" aria-label="Permalink to &quot;15.1 先建立单插件基线&quot;">​</a></h3><p>调试新地图时，先只加载一个熟悉的二维规划器，确认地图、TF、Footprint、Inflation 和 Action 链路正确，再增加 Hybrid、Lattice 或 Theta*。这样能避免接口问题与算法问题互相干扰。</p><h3 id="_15-2-多算法测试时清空强制选择" tabindex="-1">15.2 多算法测试时清空强制选择 <a class="header-anchor" href="#_15-2-多算法测试时清空强制选择" aria-label="Permalink to &quot;15.2 多算法测试时清空强制选择&quot;">​</a></h3><p>要比较 Action 中不同 <code>planner_id</code>，必须保证 <code>selected_planner</code> 为空。否则所有请求都被服务器强制到同一个插件，测试结果看似来自多种算法，实际只跑了一种。</p><h3 id="_15-3-把耗时告警与重规划频率分开配置" tabindex="-1">15.3 把耗时告警与重规划频率分开配置 <a class="header-anchor" href="#_15-3-把耗时告警与重规划频率分开配置" aria-label="Permalink to &quot;15.3 把耗时告警与重规划频率分开配置&quot;">​</a></h3><p><code>expected_planner_frequency</code> 只定义“单次规划多慢算超时告警”。真正的 <code>replan_frequency</code> 应配置在行为树或上层 Navigator。</p><p>本工程当前 <code>expected_planner_frequency=20.0</code>，对应 <code>0.05 s</code> 告警阈值；而 <code>regulated_navigator.replan_frequency=1.0</code>，表示控制期间每秒发起一次后台重规划。两者不是同一个频率。</p><h3 id="_15-4-用-action-result-做业务输入" tabindex="-1">15.4 用 Action Result 做业务输入 <a class="header-anchor" href="#_15-4-用-action-result-做业务输入" aria-label="Permalink to &quot;15.4 用 Action Result 做业务输入&quot;">​</a></h3><p><code>/plan</code> 是可视化旁路，Action Result 才是请求与结果一一对应的数据。上层要记录规划耗时、Planner ID、目标和 Path，才能在失败时还原一次完整请求。</p><h3 id="_15-5-结论" tabindex="-1">15.5 结论 <a class="header-anchor" href="#_15-5-结论" aria-label="Permalink to &quot;15.5 结论&quot;">​</a></h3><p><code>nav2_planner</code> 的价值不在于“又实现了一套 A*”，而在于把 Lifecycle、Global Costmap、TF、插件、Action、Service 和结果状态收敛成统一运行时。</p><p>真正用好它，需要同时回答几个问题：节点是否 active，Costmap 是否 current，TF 是否完整，最终 Planner ID 到底是谁，插件是否已经在 configure 阶段装载，Path 是空还是异常，以及上层是否真的在发起重规划。</p><p>把这些边界理顺后，切换算法、做 A／B 对比、定位规划卡住和解释路径差异，都会从“盲调参数”变成可验证的数据流排查。</p>',269)])])}const C=s(c,[["render",d]]);export{y as __pageData,C as default};
