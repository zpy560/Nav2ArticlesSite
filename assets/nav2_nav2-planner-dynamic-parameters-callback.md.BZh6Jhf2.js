import{_ as e,o as s,c as i,a2 as n}from"./chunks/framework.C0KMm31j.js";const t="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_01_request_flow.png",l="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_02_lifecycle.png",p="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_03_vote.png",r="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_04_frequency.png",h="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_05_selection.png",d="/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_06_atomic.png",m=JSON.parse('{"title":"一条参数命令，如何让 Nav2 当场“换脑”？逐行拆解 PlannerServer 动态回调","description":"Nav2 正在运行，能不能不重启节点，就把全局规划器从 GridBasedAstar 切换成 Smac2D？","frontmatter":{"title":"一条参数命令，如何让 Nav2 当场“换脑”？逐行拆解 PlannerServer 动态回调","description":"Nav2 正在运行，能不能不重启节点，就把全局规划器从 GridBasedAstar 切换成 Smac2D？","date":"2026-07-24","topic":"Nav2 工程实践","section":"二 · 全局规划与路径搜索","readingTime":11,"source":"2026-07-24/nav2_planner_dynamic_parameters_callback.md","status":"published","cover":"/article-assets/2026-07-24/nav2_planner_dynamic_01_request_flow.png","head":[["link",{"rel":"canonical","href":"https://zpy560.github.io/Nav2ArticlesSite/nav2/nav2-planner-dynamic-parameters-callback"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:title","content":"一条参数命令，如何让 Nav2 当场“换脑”？逐行拆解 PlannerServer 动态回调"}],["meta",{"property":"og:description","content":"Nav2 正在运行，能不能不重启节点，就把全局规划器从 GridBasedAstar 切换成 Smac2D？"}],["meta",{"property":"og:url","content":"https://zpy560.github.io/Nav2ArticlesSite/nav2/nav2-planner-dynamic-parameters-callback"}],["meta",{"property":"og:image","content":"https://zpy560.github.io/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_01_request_flow.png"}],["meta",{"property":"article:published_time","content":"2026-07-24"}],["meta",{"name":"twitter:title","content":"一条参数命令，如何让 Nav2 当场“换脑”？逐行拆解 PlannerServer 动态回调"}],["meta",{"name":"twitter:description","content":"Nav2 正在运行，能不能不重启节点，就把全局规划器从 GridBasedAstar 切换成 Smac2D？"}],["meta",{"name":"twitter:image","content":"https://zpy560.github.io/Nav2ArticlesSite/article-assets/2026-07-24/nav2_planner_dynamic_01_request_flow.png"}]],"prev":{"text":"一颗 A* 大脑，三种机器人怎么共用？拆解 nav2_smac_planner 全调用、状态与数据流","link":"/nav2/nav2-smac-planner-full-dataflow"},"next":{"text":"一个目标点要闯过多少关？逐函数拆解 Nav2 Planner Server 的 21 段数据流","link":"/nav2/nav2-planner-source-walkthrough"}},"headers":[],"relativePath":"nav2/nav2-planner-dynamic-parameters-callback.md","filePath":"nav2/nav2-planner-dynamic-parameters-callback.md","lastUpdated":null}'),c={name:"nav2/nav2-planner-dynamic-parameters-callback.md"};function o(k,a,g,u,F,_){return s(),i("div",null,[...a[0]||(a[0]=[n('<h1 id="一条参数命令-如何让-nav2-当场-换脑-逐行拆解-plannerserver-动态回调" tabindex="-1">一条参数命令，如何让 Nav2 当场“换脑”？逐行拆解 PlannerServer 动态回调 <a class="header-anchor" href="#一条参数命令-如何让-nav2-当场-换脑-逐行拆解-plannerserver-动态回调" aria-label="Permalink to &quot;一条参数命令，如何让 Nav2 当场“换脑”？逐行拆解 PlannerServer 动态回调&quot;">​</a></h1><p>Nav2 正在运行，能不能不重启节点，就把全局规划器从 <code>GridBasedAstar</code> 切换成 <code>Smac2D</code>？</p><p>当前工程确实支持这种热切换。入口不是新的 Action，也不是参数事件订阅者，而是 <code>PlannerServer::dynamicParametersCallback(std::vector&lt;rclcpp::Parameter&gt; parameters)</code>。它站在 ROS 2 参数真正提交之前：读取一批候选值、修改 Planner Server 的运行时成员，再用 <code>rcl_interfaces::msg::SetParametersResult</code> 投出“接受”或“拒绝”票。</p><p>这个函数只有三十多行，但同时连接参数服务、Lifecycle、插件表、两个规划 Action 和运行时互斥锁。它既能让机器人当场切换规划算法，也藏着一个容易被忽视的原子批量副作用。</p><p>本文基于当前本地 <code>nav2_planner 1.1.20</code>、<code>planner_server.cpp</code>、头文件、单元测试、ROS 2 Humble 参数接口和现用 YAML，只解析这一个回调的详细用法与实现边界。</p><p><img src="'+t+`" alt="参数设置进入 Planner Server 的完整数据流"></p><h2 id="一、先读懂函数签名" tabindex="-1">一、先读懂函数签名 <a class="header-anchor" href="#一、先读懂函数签名" aria-label="Permalink to &quot;一、先读懂函数签名&quot;">​</a></h2><h3 id="_1-1-返回值不是新参数-而是一张表决票" tabindex="-1">1.1 返回值不是新参数，而是一张表决票 <a class="header-anchor" href="#_1-1-返回值不是新参数-而是一张表决票" aria-label="Permalink to &quot;1.1 返回值不是新参数，而是一张表决票&quot;">​</a></h3><p>函数返回：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rcl_interfaces</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">msg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::SetParametersResult</span></span></code></pre></div><p>这个消息只有两个核心字段：</p><ul><li><code>successful</code>：本回调是否接受整批候选参数。</li><li><code>reason</code>：接受或拒绝的解释，供命令行、日志和界面显示。</li></ul><p>生成消息的默认值是 <code>successful=false</code>、<code>reason=&quot;&quot;</code>。因此源码不能只构造 <code>result</code> 后直接返回，而是在全部候选值检查完毕后显式写入：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.successful </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result;</span></span></code></pre></div><p>一旦发现非法 <code>selected_planner</code>，函数会写入拒绝原因并提前返回。这里的返回值控制参数能否提交，不是 Planner Action 的执行结果，也不携带 Path。</p><h3 id="_1-2-输入是一次设置请求的候选参数向量" tabindex="-1">1.2 输入是一次设置请求的候选参数向量 <a class="header-anchor" href="#_1-2-输入是一次设置请求的候选参数向量" aria-label="Permalink to &quot;1.2 输入是一次设置请求的候选参数向量&quot;">​</a></h3><p>输入类型是：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::vector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rclcpp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Parameter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> parameters</span></span></code></pre></div><p>使用单参数接口时，向量通常只有一个元素；使用原子批量接口时，它可以包含多个候选参数。每个 <code>rclcpp::Parameter</code> 同时保存名称、类型和值，回调通过：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">parameter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">parameter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get_name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">parameter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">as_double</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">parameter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">as_string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>识别自己关心的内容。当前签名按值接收向量，循环又写成 <code>for (auto parameter : parameters)</code>，因此会再次复制每个参数对象。把它改成常量引用可以减少复制，但不改变回调语义。</p><h2 id="二、一条命令如何进入这个回调" tabindex="-1">二、一条命令如何进入这个回调 <a class="header-anchor" href="#二、一条命令如何进入这个回调" aria-label="Permalink to &quot;二、一条命令如何进入这个回调&quot;">​</a></h2><h3 id="_2-1-参数服务先接收候选值" tabindex="-1">2.1 参数服务先接收候选值 <a class="header-anchor" href="#_2-1-参数服务先接收候选值" aria-label="Permalink to &quot;2.1 参数服务先接收候选值&quot;">​</a></h3><p>执行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Smac2D</span></span></code></pre></div><p>命令不会直接调用 <code>dynamicParametersCallback()</code>。请求先进入 Planner Server 的 ROS 2 参数服务，rclcpp 检查参数声明和静态类型，再调用已注册的 On Set Parameters Callback。此时传进函数的值仍是候选值，尚未写入节点参数存储。</p><h3 id="_2-2-回调在提交之前同步表决" tabindex="-1">2.2 回调在提交之前同步表决 <a class="header-anchor" href="#_2-2-回调在提交之前同步表决" aria-label="Permalink to &quot;2.2 回调在提交之前同步表决&quot;">​</a></h3><p>函数返回 <code>successful=true</code> 后，rclcpp 才允许参数进入存储，并发布 Parameter Event。返回 <code>successful=false</code> 时，请求方会收到 <code>reason</code>，候选值不提交。</p><p>所以它不是“参数变化后的通知”，而是“参数提交前的守门人”。这个时序非常重要：ROS 2 可以撤销尚未提交的参数存储，却不会自动撤销回调已经写入的普通 C++ 成员。</p><h3 id="_2-3-其他参数并不由这个函数负责" tabindex="-1">2.3 其他参数并不由这个函数负责 <a class="header-anchor" href="#_2-3-其他参数并不由这个函数负责" aria-label="Permalink to &quot;2.3 其他参数并不由这个函数负责&quot;">​</a></h3><p>当前回调只处理两种“类型＋名称”组合：</p><ul><li><code>double + expected_planner_frequency</code>。</li><li><code>string + selected_planner</code>。</li></ul><p>其他名称会被本函数忽略，并在循环结束时得到一张成功票。它们是否最终成功，还取决于参数是否已声明、类型是否匹配，以及同一节点上其他参数回调是否接受。特别是 <code>planner_plugins</code> 即使参数存储发生变化，本函数也不会创建、配置或激活新插件。</p><h2 id="三、lifecycle-决定回调是否存在" tabindex="-1">三、Lifecycle 决定回调是否存在 <a class="header-anchor" href="#三、lifecycle-决定回调是否存在" aria-label="Permalink to &quot;三、Lifecycle 决定回调是否存在&quot;">​</a></h2><h3 id="_3-1-构造阶段只声明参数" tabindex="-1">3.1 构造阶段只声明参数 <a class="header-anchor" href="#_3-1-构造阶段只声明参数" aria-label="Permalink to &quot;3.1 构造阶段只声明参数&quot;">​</a></h3><p><code>PlannerServer()</code> 声明：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>selected_planner = &quot;&quot;</span></span>
<span class="line"><span>expected_planner_frequency = 1.0</span></span></code></pre></div><p>此时 <code>planners_</code> 还没有加载完成，动态回调也没有注册。</p><h3 id="_3-2-configure-读取初值并建立插件表" tabindex="-1">3.2 configure 读取初值并建立插件表 <a class="header-anchor" href="#_3-2-configure-读取初值并建立插件表" aria-label="Permalink to &quot;3.2 configure 读取初值并建立插件表&quot;">​</a></h3><p><code>on_configure()</code> 根据 <code>planner_plugins</code> 创建全部 Global Planner 实例，再读取 <code>selected_planner</code> 和 <code>expected_planner_frequency</code>。现用主要配置把频率设为 <code>20.0 Hz</code>，并预加载 <code>GridBased</code>、<code>GridBasedAstar</code>、<code>Smac2D</code>、<code>SmacHybrid</code>、<code>SmacLattice</code> 和 <code>ThetaStar</code>。</p><p>如果启动 YAML 中的 <code>selected_planner</code> 不在 <code>planners_</code>，configure 只打印警告，不会清空非法值，也不会让 Lifecycle 配置失败。这比运行期回调宽松。</p><h3 id="_3-3-active-阶段才注册回调" tabindex="-1">3.3 active 阶段才注册回调 <a class="header-anchor" href="#_3-3-active-阶段才注册回调" aria-label="Permalink to &quot;3.3 active 阶段才注册回调&quot;">​</a></h3><p><img src="`+l+`" alt="Lifecycle 状态与动态回调有效期"></p><p><code>on_activate()</code> 先激活 Action Server、Costmap 和全部 Planner 插件，随后调用：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dyn_params_handler_ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> node-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add_on_set_parameters_callback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PlannerServer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::dynamicParametersCallback, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, _1));</span></span></code></pre></div><p>返回的句柄必须保存在 <code>dyn_params_handler_</code>。句柄生命周期就是回调生命周期；如果不保存，共享指针释放后，回调也会失效。</p><h3 id="_3-4-deactivate-会注销回调" tabindex="-1">3.4 deactivate 会注销回调 <a class="header-anchor" href="#_3-4-deactivate-会注销回调" aria-label="Permalink to &quot;3.4 deactivate 会注销回调&quot;">​</a></h3><p><code>on_deactivate()</code> 执行 <code>dyn_params_handler_.reset()</code>。节点基础参数服务仍然存在，因此 inactive 期间参数存储仍可能被修改，但 <code>selected_planner_id_</code> 和 <code>max_planner_duration_</code> 不会经过本函数同步。</p><p>单纯重新 activate 只会重新注册回调，不会补读 inactive 期间的参数。要重新按参数存储建立运行时状态，需要让节点重新经过 cleanup 和 configure。</p><p>运行期修改前应先检查：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lifecycle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> expected_planner_frequency</span></span></code></pre></div><h2 id="四、互斥锁保证一次规划不会中途换算法" tabindex="-1">四、互斥锁保证一次规划不会中途换算法 <a class="header-anchor" href="#四、互斥锁保证一次规划不会中途换算法" aria-label="Permalink to &quot;四、互斥锁保证一次规划不会中途换算法&quot;">​</a></h2><h3 id="_4-1-回调入口先锁住-server-级状态" tabindex="-1">4.1 回调入口先锁住 Server 级状态 <a class="header-anchor" href="#_4-1-回调入口先锁住-server-级状态" aria-label="Permalink to &quot;4.1 回调入口先锁住 Server 级状态&quot;">​</a></h3><p><img src="`+p+'" alt="参数类型表决与规划互斥"></p><p>函数开头立即创建：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lock_guard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mutex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dynamic_params_lock_</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>这把锁保护的不是 ROS 2 参数存储，而是 Planner Server 自己的运行时成员和规划过程。</p><h3 id="_4-2-两个规划-action-使用同一把锁" tabindex="-1">4.2 两个规划 Action 使用同一把锁 <a class="header-anchor" href="#_4-2-两个规划-action-使用同一把锁" aria-label="Permalink to &quot;4.2 两个规划 Action 使用同一把锁&quot;">​</a></h3><p><code>computePlan()</code> 与 <code>computePlanThroughPoses()</code> 入口也持有 <code>dynamic_params_lock_</code>，并一直覆盖 Costmap 等待、TF 变换、插件 <code>createPlan()</code>、路径校验和 Action 结束。</p><p>因此当前规划执行时，参数更新会等待；参数回调执行时，新的规划请求也会等待。一次规划不会在 <code>createPlan()</code> 中途突然切换 Planner，也不会在结束前改变耗时告警阈值。</p><p>代价是参数服务延迟与规划耗时直接相关。若插件规划很慢，<code>ros2 param set</code> 看起来也会“卡住”，这不是参数服务失效，而是在等待同一把锁。</p><h2 id="五、expected-planner-frequency-实际只控制告警" tabindex="-1">五、<code>expected_planner_frequency</code> 实际只控制告警 <a class="header-anchor" href="#五、expected-planner-frequency-实际只控制告警" aria-label="Permalink to &quot;五、`expected_planner_frequency` 实际只控制告警&quot;">​</a></h2><h3 id="_5-1-正值会换算成最大规划时长" tabindex="-1">5.1 正值会换算成最大规划时长 <a class="header-anchor" href="#_5-1-正值会换算成最大规划时长" aria-label="Permalink to &quot;5.1 正值会换算成最大规划时长&quot;">​</a></h3><p><img src="'+r+'" alt="频率参数到耗时告警阈值的数据流"></p><p>正值采用：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>max_planner_duration = 1 / expected_planner_frequency</span></span></code></pre></div><p>例如：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>20.0 Hz → 0.05 s</span></span>\n<span class="line"><span>5.0 Hz  → 0.20 s</span></span></code></pre></div><p>两个规划 Action 完成后都会比较 <code>planning_time</code> 与该阈值。超时只打印 <code>Planner loop missed its desired rate</code>，不会取消 Path，也不会自动重试。</p><h3 id="_5-2-它不会改变重规划周期" tabindex="-1">5.2 它不会改变重规划周期 <a class="header-anchor" href="#_5-2-它不会改变重规划周期" aria-label="Permalink to &quot;5.2 它不会改变重规划周期&quot;">​</a></h3><p>这个参数不会创建 Wall Timer，不会改变 Behavior Tree 的 <code>RateController</code>，也不会主动发起新的 ComputePath Action。全局规划多久调用一次，仍由 BT 或其他 Action Client 决定。</p><p>名字中的 frequency 表示期望性能基线，不是 Planner Server 内部执行频率。</p><h3 id="_5-3-非正值会关闭告警但仍然成功" tabindex="-1">5.3 非正值会关闭告警但仍然成功 <a class="header-anchor" href="#_5-3-非正值会关闭告警但仍然成功" aria-label="Permalink to &quot;5.3 非正值会关闭告警但仍然成功&quot;">​</a></h3><p>当候选值小于或等于零时，函数打印警告，把 <code>max_planner_duration_</code> 设为 <code>0.0</code>，最后仍返回 <code>successful=true</code>。因为后续判断写成：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (max_planner_duration_ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cycle_duration.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> max_planner_duration_)</span></span></code></pre></div><p>零值会直接关闭耗时越界告警。</p><p>实际使用：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> expected_planner_frequency</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5.0</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> expected_planner_frequency</span></span></code></pre></div><p>参数已声明为 double，命令中使用 <code>5.0</code>，不要写成整数 <code>5</code>。</p><h2 id="六、selected-planner-如何让-nav2-在线换脑" tabindex="-1">六、<code>selected_planner</code> 如何让 Nav2 在线换脑 <a class="header-anchor" href="#六、selected-planner-如何让-nav2-在线换脑" aria-label="Permalink to &quot;六、`selected_planner` 如何让 Nav2 在线换脑&quot;">​</a></h2><h3 id="_6-1-候选值必须是已加载的插件-id" tabindex="-1">6.1 候选值必须是已加载的插件 ID <a class="header-anchor" href="#_6-1-候选值必须是已加载的插件-id" aria-label="Permalink to &quot;6.1 候选值必须是已加载的插件 ID&quot;">​</a></h3><p><img src="'+h+`" alt="selected_planner 与 Action planner_id 的优先级"></p><p>非空候选值会在 <code>planners_</code> 中查找。这里需要的是插件 ID，例如 <code>Smac2D</code>，不是 pluginlib 类型名 <code>nav2_smac_planner/SmacPlanner2D</code>。</p><p>现用配置已加载六个 ID：</p><ul><li><code>GridBased</code>。</li><li><code>GridBasedAstar</code>。</li><li><code>Smac2D</code>。</li><li><code>SmacHybrid</code>。</li><li><code>SmacLattice</code>。</li><li><code>ThetaStar</code>。</li></ul><p>切换命令：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Smac2D</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><p>成功后，下一次规划请求读取新的 <code>selected_planner_id_</code>。</p><h3 id="_6-2-非空-server-参数覆盖-action-goal" tabindex="-1">6.2 非空 Server 参数覆盖 Action Goal <a class="header-anchor" href="#_6-2-非空-server-参数覆盖-action-goal" aria-label="Permalink to &quot;6.2 非空 Server 参数覆盖 Action Goal&quot;">​</a></h3><p><code>getPlan()</code> 的实际选择公式是：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">requested_planner_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> selected_planner_id_.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">empty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  planner_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> selected_planner_id_;</span></span></code></pre></div><p>只要 Server 参数非空，即使 Action Goal 明确请求 <code>ThetaStar</code>，也会使用 Server 选择的 <code>Smac2D</code>。源码会输出一次警告，说明 Action 请求被配置值覆盖。</p><h3 id="_6-3-空字符串把选择权还给-action" tabindex="-1">6.3 空字符串把选择权还给 Action <a class="header-anchor" href="#_6-3-空字符串把选择权还给-action" aria-label="Permalink to &quot;6.3 空字符串把选择权还给 Action&quot;">​</a></h3><p>恢复每个 Action Goal 自己选择 Planner：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&#39;&#39;&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><p>空字符串会合法写入 <code>selected_planner_id_</code>。后续 <code>getPlan()</code> 重新读取 Goal 的 <code>planner_id</code>；如果 Server 只加载一个插件，还保留单插件回退逻辑。</p><h3 id="_6-4-不存在的-id-会被拒绝" tabindex="-1">6.4 不存在的 ID 会被拒绝 <a class="header-anchor" href="#_6-4-不存在的-id-会被拒绝" aria-label="Permalink to &quot;6.4 不存在的 ID 会被拒绝&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> NotLoaded</span></span></code></pre></div><p>回调返回：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>successful = false</span></span>
<span class="line"><span>reason = &quot;selected_planner is not configured&quot;</span></span></code></pre></div><p>参数存储保持原值，运行时成员也不会在这个拒绝分支被覆盖。这个检查只能切换已存在于 <code>planners_</code> 的实例，不能热加载新插件。</p><h2 id="七、为什么修改-planner-plugins-不会热加载" tabindex="-1">七、为什么修改 <code>planner_plugins</code> 不会热加载 <a class="header-anchor" href="#七、为什么修改-planner-plugins-不会热加载" aria-label="Permalink to &quot;七、为什么修改 \`planner_plugins\` 不会热加载&quot;">​</a></h2><h3 id="_7-1-插件实例只在-configure-创建" tabindex="-1">7.1 插件实例只在 configure 创建 <a class="header-anchor" href="#_7-1-插件实例只在-configure-创建" aria-label="Permalink to &quot;7.1 插件实例只在 configure 创建&quot;">​</a></h3><p><code>planner_plugins</code> 决定插件 ID 列表，<code>&lt;id&gt;.plugin</code> 决定 pluginlib 类型。它们在构造和 configure 阶段被读取，插件随后经过 create、configure 和 activate。</p><p>动态回调没有处理这两类参数，也没有调用 ClassLoader。因此运行期执行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> planner_plugins</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;[&#39;Smac2D&#39;]&quot;</span></span></code></pre></div><p>即使参数服务接受了存储变化，<code>planners_</code> 也不会随之重建。可靠做法是修改配置后重新执行完整 Lifecycle 配置流程。</p><h3 id="_7-2-server-选择与插件装载必须分开理解" tabindex="-1">7.2 Server 选择与插件装载必须分开理解 <a class="header-anchor" href="#_7-2-server-选择与插件装载必须分开理解" aria-label="Permalink to &quot;7.2 Server 选择与插件装载必须分开理解&quot;">​</a></h3><p><code>selected_planner</code> 是“从已加载实例里选谁”，<code>planner_plugins</code> 是“configure 时要创建谁”。前者适合热更新，后者属于资源结构变化，不能由这一个回调完成。</p><h2 id="八、原子批量提交仍可能留下成员副作用" tabindex="-1">八、原子批量提交仍可能留下成员副作用 <a class="header-anchor" href="#八、原子批量提交仍可能留下成员副作用" aria-label="Permalink to &quot;八、原子批量提交仍可能留下成员副作用&quot;">​</a></h2><h3 id="_8-1-参数存储的原子性不覆盖普通成员" tabindex="-1">8.1 参数存储的原子性不覆盖普通成员 <a class="header-anchor" href="#_8-1-参数存储的原子性不覆盖普通成员" aria-label="Permalink to &quot;8.1 参数存储的原子性不覆盖普通成员&quot;">​</a></h3><p><img src="`+d+`" alt="原子批量参数与 C++ 成员副作用"></p><p>假设原子请求按如下顺序传入：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>expected_planner_frequency = 5.0</span></span>
<span class="line"><span>selected_planner = NotLoaded</span></span></code></pre></div><p>循环先把 <code>max_planner_duration_</code> 改成 <code>0.2 s</code>，随后发现 Planner ID 非法并返回失败。ROS 2 参数存储会拒绝整批候选值，但不会把普通 C++ 成员恢复成旧阈值。</p><p>于是可能出现：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>参数存储仍是 20.0 Hz</span></span>
<span class="line"><span>运行时阈值却已经是 0.2 s</span></span></code></pre></div><p>如果其他参数回调在本函数返回成功后拒绝整批请求，也可能留下相同类型的副作用。</p><h3 id="_8-2-安全使用应采用单参数更新" tabindex="-1">8.2 安全使用应采用单参数更新 <a class="header-anchor" href="#_8-2-安全使用应采用单参数更新" aria-label="Permalink to &quot;8.2 安全使用应采用单参数更新&quot;">​</a></h3><p>当前实现下，Server 级参数宜一次修改一个，并立即读取验证：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Smac2D</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><p>随后触发一次规划，通过日志、Path 形态或插件调试信息确认实际 Planner。</p><h3 id="_8-3-源码修复应采用两阶段提交" tabindex="-1">8.3 源码修复应采用两阶段提交 <a class="header-anchor" href="#_8-3-源码修复应采用两阶段提交" aria-label="Permalink to &quot;8.3 源码修复应采用两阶段提交&quot;">​</a></h3><p>更稳妥的实现方式是先把候选频率和 Planner ID 放入局部变量，完成整批类型与合法性检查；只有所有候选值都通过，才一次性更新 <code>max_planner_duration_</code> 和 <code>selected_planner_id_</code>。</p><p>这样 <code>SetParametersResult</code> 的成功与成员提交形成同一个事务边界。</p><h2 id="九、现有单元测试覆盖了什么" tabindex="-1">九、现有单元测试覆盖了什么 <a class="header-anchor" href="#九、现有单元测试覆盖了什么" aria-label="Permalink to &quot;九、现有单元测试覆盖了什么&quot;">​</a></h2><h3 id="_9-1-已覆盖正频率与负频率写入" tabindex="-1">9.1 已覆盖正频率与负频率写入 <a class="header-anchor" href="#_9-1-已覆盖正频率与负频率写入" aria-label="Permalink to &quot;9.1 已覆盖正频率与负频率写入&quot;">​</a></h3><p><code>test_dynamic_parameters.cpp</code> 通过 <code>AsyncParametersClient::set_parameters_atomically()</code> 写入 <code>100.0</code> 和 <code>-1.0</code>，随后读取参数存储，确认两者都能提交。这与“非正值关闭告警但仍返回成功”的实现一致。</p><h3 id="_9-2-shim-丢弃了真实回调结果" tabindex="-1">9.2 Shim 丢弃了真实回调结果 <a class="header-anchor" href="#_9-2-shim-丢弃了真实回调结果" aria-label="Permalink to &quot;9.2 Shim 丢弃了真实回调结果&quot;">​</a></h3><p>测试中的 <code>dynamicParamsShim()</code> 先把自己的 <code>result.successful</code> 设为 true，再调用 <code>dynamicParametersCallback(parameters)</code>，但没有接住并返回真实结果：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dynamicParametersCallback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(parameters);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result;</span></span></code></pre></div><p>因此即使未来加入非法 Planner 用例，这个 Shim 也会把真实拒绝覆盖成成功。测试还没有断言 <code>max_planner_duration_</code>，所以只证明参数存储变化，没有证明运行时阈值同步。</p><h3 id="_9-3-仍缺少四类关键回归" tabindex="-1">9.3 仍缺少四类关键回归 <a class="header-anchor" href="#_9-3-仍缺少四类关键回归" aria-label="Permalink to &quot;9.3 仍缺少四类关键回归&quot;">​</a></h3><ul><li>非法 <code>selected_planner</code> 必须返回失败和明确原因。</li><li>空字符串必须恢复 Action Goal 选择权。</li><li>inactive 阶段写参数后重新 activate 的成员漂移。</li><li>原子批量失败时成员不得保留局部副作用。</li></ul><p>这些用例比继续增加普通成功值更能守住回调的事务边界。</p><h2 id="十、运行时怎么安全使用" tabindex="-1">十、运行时怎么安全使用 <a class="header-anchor" href="#十、运行时怎么安全使用" aria-label="Permalink to &quot;十、运行时怎么安全使用&quot;">​</a></h2><h3 id="_10-1-切换前确认-lifecycle-与可用-id" tabindex="-1">10.1 切换前确认 Lifecycle 与可用 ID <a class="header-anchor" href="#_10-1-切换前确认-lifecycle-与可用-id" aria-label="Permalink to &quot;10.1 切换前确认 Lifecycle 与可用 ID&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lifecycle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> planner_plugins</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><p>只有 active 状态下，本回调才会同步运行时成员。目标 ID 必须已经出现在 <code>planner_plugins</code> 对应的加载集合中。</p><h3 id="_10-2-切换后同时检查存储与行为" tabindex="-1">10.2 切换后同时检查存储与行为 <a class="header-anchor" href="#_10-2-切换后同时检查存储与行为" aria-label="Permalink to &quot;10.2 切换后同时检查存储与行为&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Smac2D</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ros2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> param</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /planner_server</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> selected_planner</span></span></code></pre></div><p>参数读取相等，只能证明 ROS 2 存储已提交；还应触发一次 ComputePath 请求，确认新插件真实执行。</p><h3 id="_10-3-排障时先区分三种失败" tabindex="-1">10.3 排障时先区分三种失败 <a class="header-anchor" href="#_10-3-排障时先区分三种失败" aria-label="Permalink to &quot;10.3 排障时先区分三种失败&quot;">​</a></h3><ul><li>命令立即报类型错误：候选值没有通过参数类型检查。</li><li>返回 <code>selected_planner is not configured</code>：ID 未加载。</li><li>命令长时间等待：规划 Action 可能正持有 <code>dynamic_params_lock_</code>。</li></ul><p>这三种现象分别属于参数层、插件表和并发互斥，处理方向完全不同。</p><h2 id="十一、这个回调真正控制了什么" tabindex="-1">十一、这个回调真正控制了什么 <a class="header-anchor" href="#十一、这个回调真正控制了什么" aria-label="Permalink to &quot;十一、这个回调真正控制了什么&quot;">​</a></h2><h3 id="_11-1-它控制两项-server-级运行时策略" tabindex="-1">11.1 它控制两项 Server 级运行时策略 <a class="header-anchor" href="#_11-1-它控制两项-server-级运行时策略" aria-label="Permalink to &quot;11.1 它控制两项 Server 级运行时策略&quot;">​</a></h3><p><code>selected_planner_id_</code> 决定下一次规划优先使用哪个已加载插件；<code>max_planner_duration_</code> 决定何时打印规划耗时越界告警。</p><h3 id="_11-2-它不控制四类结构行为" tabindex="-1">11.2 它不控制四类结构行为 <a class="header-anchor" href="#_11-2-它不控制四类结构行为" aria-label="Permalink to &quot;11.2 它不控制四类结构行为&quot;">​</a></h3><ul><li>不加载或卸载 Planner 插件。</li><li>不改变 Behavior Tree 的重规划周期。</li><li>不让正在执行的规划中途换算法。</li><li>不自动回滚已经写入的 C++ 成员副作用。</li></ul><h2 id="十二、结语" tabindex="-1">十二、结语 <a class="header-anchor" href="#十二、结语" aria-label="Permalink to &quot;十二、结语&quot;">​</a></h2><p><code>dynamicParametersCallback()</code> 的本质不是“收到参数后改两个变量”，而是 Planner Server 的运行时事务入口。</p><p>它用 <code>SetParametersResult</code> 决定参数存储是否提交，用 Lifecycle 句柄限定热更新有效期，用 <code>dynamic_params_lock_</code> 保证规划与更新串行，再用 <code>selected_planner_id_</code> 和 <code>max_planner_duration_</code> 改变下一次规划的插件选择与耗时告警。</p><p>真正安全的使用方式也由此变得清晰：只在 active 状态修改，一次更新一个 Server 参数，只选择已经加载的插件，设置后同时检查参数存储和规划行为。若要从源码消除批量副作用，则必须把“校验候选值”和“提交运行时成员”拆成两个阶段。</p>`,154)])])}const v=e(c,[["render",o]]);export{m as __pageData,v as default};
