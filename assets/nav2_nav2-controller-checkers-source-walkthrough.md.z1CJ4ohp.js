import{_ as a,o as s,c as o,a2 as i}from"./chunks/framework.C0KMm31j.js";const l="/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_01_control_loop.png",t="/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_02_progress_window.png",c="/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_03_goal_family.png",r="/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_04_simple_goal_state.png",n="/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_05_stopped_goal.png",d="/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_06_parameters_tests.png",b=JSON.parse('{"title":"机器人明明在动，却被 Nav2 判定“卡住”？拆解 5 个进展与到点检查器","description":"机器人原地转向时明明一直在动，Nav2 却可能抛出 Failed to make progress；机器人已经进入目标点附近，FollowPath 又可能迟迟不结束。问题往往不在控制器有没有算出速度，而在 Controller Server 内部还有两道判定：ProgressChecker 保护导航","frontmatter":{"title":"机器人明明在动，却被 Nav2 判定“卡住”？拆解 5 个进展与到点检查器","description":"机器人原地转向时明明一直在动，Nav2 却可能抛出 Failed to make progress；机器人已经进入目标点附近，FollowPath 又可能迟迟不结束。问题往往不在控制器有没有算出速度，而在 Controller Server 内部还有两道判定：ProgressChecker 保护导航","date":"2026-08-10","section":"三 · 局部控制与速度生成","readingTime":15,"source":"2026-08-10/nav2_controller_checkers_source_walkthrough.md","status":"published","cover":"/article-assets/2026-08-10/controller_checkers_01_control_loop.png","head":[["link",{"rel":"canonical","href":"https://zpy560.github.io/Nav2ArticlesSite/nav2/nav2-controller-checkers-source-walkthrough"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:title","content":"机器人明明在动，却被 Nav2 判定“卡住”？拆解 5 个进展与到点检查器"}],["meta",{"property":"og:description","content":"机器人原地转向时明明一直在动，Nav2 却可能抛出 Failed to make progress；机器人已经进入目标点附近，FollowPath 又可能迟迟不结束。问题往往不在控制器有没有算出速度，而在 Controller Server 内部还有两道判定：ProgressChecker 保护导航"}],["meta",{"property":"og:url","content":"https://zpy560.github.io/Nav2ArticlesSite/nav2/nav2-controller-checkers-source-walkthrough"}],["meta",{"property":"og:image","content":"https://zpy560.github.io/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_01_control_loop.png"}],["meta",{"property":"article:published_time","content":"2026-08-10"}],["meta",{"name":"twitter:title","content":"机器人明明在动，却被 Nav2 判定“卡住”？拆解 5 个进展与到点检查器"}],["meta",{"name":"twitter:description","content":"机器人原地转向时明明一直在动，Nav2 却可能抛出 Failed to make progress；机器人已经进入目标点附近，FollowPath 又可能迟迟不结束。问题往往不在控制器有没有算出速度，而在 Controller Server 内部还有两道判定：ProgressChecker 保护导航"}],["meta",{"name":"twitter:image","content":"https://zpy560.github.io/Nav2ArticlesSite/article-assets/2026-08-10/controller_checkers_01_control_loop.png"}]],"prev":{"text":"先别急着走：Nav2 Rotation Shim 如何在路径跟踪前把方向“扶正”？","link":"/nav2/nav2-rotation-shim-controller-core-algorithm"},"next":{"text":"一束激光怎样变成避障代价？拆解 `nav2_costmap_2d` 调用全数据流","link":"/nav2/nav2-costmap-2d-full-dataflow"}},"headers":[],"relativePath":"nav2/nav2-controller-checkers-source-walkthrough.md","filePath":"nav2/nav2-controller-checkers-source-walkthrough.md","lastUpdated":null}'),h={name:"nav2/nav2-controller-checkers-source-walkthrough.md"};function p(k,e,_,g,u,m){return s(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="机器人明明在动-却被-nav2-判定-卡住-拆解-5-个进展与到点检查器" tabindex="-1">机器人明明在动，却被 Nav2 判定“卡住”？拆解 5 个进展与到点检查器 <a class="header-anchor" href="#机器人明明在动-却被-nav2-判定-卡住-拆解-5-个进展与到点检查器" aria-label="Permalink to &quot;机器人明明在动，却被 Nav2 判定“卡住”？拆解 5 个进展与到点检查器&quot;">​</a></h1><p>机器人原地转向时明明一直在动，Nav2 却可能抛出 <code>Failed to make progress</code>；机器人已经进入目标点附近，FollowPath 又可能迟迟不结束。问题往往不在控制器有没有算出速度，而在 Controller Server 内部还有两道判定：ProgressChecker 保护导航过程，GoalChecker 定义任务终点。</p><p>本文以当前 <code>learningnav2/nav2_controller</code> 源码为基线，沿着 5 个插件的头文件、实现、<code>plugins.xml</code>、Controller Server 调用点和两组 GTest，把每个函数的职责、输入输出、内部状态、参数边界和选型方法串成一条完整数据流。这里讨论的是源码实际语义，不把参数名当成结论，也不把“位置到了”误写成“机器人已经停稳”。</p><h2 id="一、两个问题-两类检查器" tabindex="-1">一、两个问题，两类检查器 <a class="header-anchor" href="#一、两个问题-两类检查器" aria-label="Permalink to &quot;一、两个问题，两类检查器&quot;">​</a></h2><h3 id="_1-1-progresschecker-保护的是导航过程" tabindex="-1">1.1 ProgressChecker 保护的是导航过程 <a class="header-anchor" href="#_1-1-progresschecker-保护的是导航过程" aria-label="Permalink to &quot;1.1 ProgressChecker 保护的是导航过程&quot;">​</a></h3><p>ProgressChecker 回答的是“机器人在允许时间内是否取得了足够进展”。Controller Server 在每次调用局部控制器计算速度之前，把当前机器人位姿传给 <code>check()</code>。如果它返回 <code>false</code>，控制循环会抛出 <code>PlannerException(&quot;Failed to make progress&quot;)</code>，当前 FollowPath 进入失败路径。</p><p>这类插件不判断目标是否到达，也不关心局部控制器输出了什么速度。它只观察位姿相对内部基准是否发生了足够变化，以及这次变化是否发生在允许时间窗内。</p><h3 id="_1-2-goalchecker-定义的是任务终点" tabindex="-1">1.2 GoalChecker 定义的是任务终点 <a class="header-anchor" href="#_1-2-goalchecker-定义的是任务终点" aria-label="Permalink to &quot;1.2 GoalChecker 定义的是任务终点&quot;">​</a></h3><p>GoalChecker 回答的是“当前位姿和速度是否满足本任务的终止条件”。Controller Server 在一个控制周期完成速度计算与发布后，读取当前位姿、路径终点和经过最小速度阈值处理的里程计速度，再调用当前 GoalChecker 的 <code>isGoalReached()</code>。</p><p>返回 <code>true</code> 不只是一个提示，而会直接让 FollowPath 控制循环结束。PositionGoalChecker、SimpleGoalChecker 和 StoppedGoalChecker 对“完成”的定义不同，所以同一台机器人、同一个目标点，换一个 GoalChecker 就可能得到完全不同的结束时刻。</p><h3 id="_1-3-它们都是进程内插件" tabindex="-1">1.3 它们都是进程内插件 <a class="header-anchor" href="#_1-3-它们都是进程内插件" aria-label="Permalink to &quot;1.3 它们都是进程内插件&quot;">​</a></h3><p>5 个实现通过 <code>PLUGINLIB_EXPORT_CLASS</code> 导出，并在 <code>plugins.xml</code> 中声明基类类型。Controller Server 使用 <code>pluginlib::ClassLoader</code> 创建它们，因此这些检查器没有独立 ROS 节点，不发布 Topic，不订阅传感器，也不会生成 <code>/cmd_vel</code>。</p><p>它们拿到的数据来自 Controller Server：ProgressChecker 得到当前 <code>PoseStamped</code>；GoalChecker 得到当前 Pose、目标 Pose 和当前 Twist。真正的位姿获取、TF 变换、里程计订阅和控制循环调度都在 Controller Server 中完成。</p><p><img src="'+l+'" alt="图 1：Controller Server 控制循环中两类检查器的调用位置"></p><h2 id="二、simpleprogresschecker-一段会不断滚动的移动时间窗" tabindex="-1">二、SimpleProgressChecker：一段会不断滚动的移动时间窗 <a class="header-anchor" href="#二、simpleprogresschecker-一段会不断滚动的移动时间窗" aria-label="Permalink to &quot;二、SimpleProgressChecker：一段会不断滚动的移动时间窗&quot;">​</a></h2><h3 id="_2-1-initialize-只准备规则-不建立基准" tabindex="-1">2.1 <code>initialize()</code> 只准备规则，不建立基准 <a class="header-anchor" href="#_2-1-initialize-只准备规则-不建立基准" aria-label="Permalink to &quot;2.1 `initialize()` 只准备规则，不建立基准&quot;">​</a></h3><p><code>initialize(parent, plugin_name)</code> 从 Controller Server 的 LifecycleNode 获取时钟，声明并读取两个插件私有参数：</p><ul><li><code>required_movement_radius</code>：构成有效进展的最小 XY 位移，默认 <code>0.5 m</code>。</li><li><code>movement_time_allowance</code>：未达到最小位移时允许持续运行的最长时间，默认 <code>10.0 s</code>。</li></ul><p>函数还会注册 <code>dynamicParametersCallback()</code>。初始化阶段没有当前机器人位姿，因此它不会设置基准点；真正的基准位姿和基准时间要等首次 <code>check()</code> 才建立。</p><h3 id="_2-2-check-同时维护距离窗口和时间窗口" tabindex="-1">2.2 <code>check()</code> 同时维护距离窗口和时间窗口 <a class="header-anchor" href="#_2-2-check-同时维护距离窗口和时间窗口" aria-label="Permalink to &quot;2.2 `check()` 同时维护距离窗口和时间窗口&quot;">​</a></h3><p><code>check(current_pose)</code> 先把三维 Pose 转成平面 <code>x、y、theta</code>，随后检查基准是否存在。首次调用时 <code>baseline_pose_set_</code> 为 <code>false</code>，短路求值会阻止代码读取尚未初始化的基准位姿，函数直接调用 <code>resetBaselinePose()</code>，并返回 <code>true</code>。</p><p>基准有效后，判定可写成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>distance = hypot(current.x - baseline.x, current.y - baseline.y)</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>distance &gt; required_movement_radius</span></span>\n<span class="line"><span>    -&gt; 刷新基准位姿和基准时间，返回 true</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>distance &lt;= required_movement_radius</span></span>\n<span class="line"><span>    -&gt; elapsed &lt;= movement_time_allowance 时返回 true</span></span>\n<span class="line"><span>    -&gt; elapsed &gt; movement_time_allowance 时返回 false</span></span></code></pre></div><p>这里有两个很关键的边界。位移使用严格大于，恰好等于 <code>required_movement_radius</code> 不会刷新基准；时间只有严格超过 <code>movement_time_allowance</code> 才失败，恰好位于时间边界时仍返回 <code>true</code>。</p><h3 id="_2-3-reset-与-resetbaselinepose-不是同一件事" tabindex="-1">2.3 <code>reset()</code> 与 <code>resetBaselinePose()</code> 不是同一件事 <a class="header-anchor" href="#_2-3-reset-与-resetbaselinepose-不是同一件事" aria-label="Permalink to &quot;2.3 `reset()` 与 `resetBaselinePose()` 不是同一件事&quot;">​</a></h3><p><code>reset()</code> 只把 <code>baseline_pose_set_</code> 清为 <code>false</code>，不改参数，也不立即采样机器人位姿。新 FollowPath 任务开始后，下一次 <code>check()</code> 才会从真实当前位置重新计时。</p><p><code>resetBaselinePose(pose)</code> 则在一次操作中写入 <code>baseline_pose_</code>、<code>baseline_time_</code> 和有效标志。把位姿与时间同时更新非常重要，否则距离窗口和时间窗口会来自不同采样时刻。</p><h3 id="_2-4-isrobotmovedenough-与-pose-distance-明确忽略朝向" tabindex="-1">2.4 <code>isRobotMovedEnough()</code> 与 <code>pose_distance()</code> 明确忽略朝向 <a class="header-anchor" href="#_2-4-isrobotmovedenough-与-pose-distance-明确忽略朝向" aria-label="Permalink to &quot;2.4 `isRobotMovedEnough()` 与 `pose_distance()` 明确忽略朝向&quot;">​</a></h3><p><code>pose_distance()</code> 使用 <code>std::hypot(dx, dy)</code> 计算 XY 欧氏距离，<code>isRobotMovedEnough()</code> 再判断该距离是否严格大于 <code>radius_</code>。虽然输入被转换成包含 <code>theta</code> 的 <code>Pose2D</code>，这两个函数都不读取朝向。</p><p>因此，机器人即使持续原地旋转，只要 XY 累计变化没有超过半径，SimpleProgressChecker 仍会在时间窗结束后判定“没有进展”。这不是 TF 丢失，也不是控制器没发速度，而是插件对“进展”的定义本来就只有平移。</p><h3 id="_2-5-dynamicparameterscallback-立即改阈值-但不重开窗口" tabindex="-1">2.5 <code>dynamicParametersCallback()</code> 立即改阈值，但不重开窗口 <a class="header-anchor" href="#_2-5-dynamicparameterscallback-立即改阈值-但不重开窗口" aria-label="Permalink to &quot;2.5 `dynamicParametersCallback()` 立即改阈值，但不重开窗口&quot;">​</a></h3><p>动态回调只处理本插件命名空间下的 double 参数。修改半径会直接写入 <code>radius_</code>，修改允许时间会转换为新的 ROS Duration。回调不会调用 <code>reset()</code>，所以新阈值立即作用于当前基准窗口。</p><p>运行中把允许时间从 <code>10 s</code> 缩短到 <code>2 s</code> 时，如果当前窗口已经持续超过 <code>2 s</code>，下一次 <code>check()</code> 就可能立刻失败；把移动半径调大，也不会把基准时间重新归零。</p><p><img src="'+t+'" alt="图 2：Simple 与 Pose ProgressChecker 的基准窗口和超时状态机"></p><h2 id="三、poseprogresschecker-让原地转向也能证明-我在工作" tabindex="-1">三、PoseProgressChecker：让原地转向也能证明“我在工作” <a class="header-anchor" href="#三、poseprogresschecker-让原地转向也能证明-我在工作" aria-label="Permalink to &quot;三、PoseProgressChecker：让原地转向也能证明“我在工作”&quot;">​</a></h2><h3 id="_3-1-继承父类时间窗-只扩展有效进展条件" tabindex="-1">3.1 继承父类时间窗，只扩展有效进展条件 <a class="header-anchor" href="#_3-1-继承父类时间窗-只扩展有效进展条件" aria-label="Permalink to &quot;3.1 继承父类时间窗，只扩展有效进展条件&quot;">​</a></h3><p>PoseProgressChecker 继承 SimpleProgressChecker，继续使用父类的时钟、平移半径、允许时间、基准位姿、基准时间和 <code>resetBaselinePose()</code>。它新增的核心参数只有 <code>required_movement_angle</code>，默认值为 <code>0.5 rad</code>。</p><p><code>initialize()</code> 先调用父类初始化，再声明并读取角度阈值。父类动态回调负责 <code>required_movement_radius</code> 与 <code>movement_time_allowance</code>，子类动态回调负责 <code>required_movement_angle</code>。</p><h3 id="_3-2-check-把平移和旋转组成逻辑或" tabindex="-1">3.2 <code>check()</code> 把平移和旋转组成逻辑或 <a class="header-anchor" href="#_3-2-check-把平移和旋转组成逻辑或" aria-label="Permalink to &quot;3.2 `check()` 把平移和旋转组成逻辑或&quot;">​</a></h3><p>它的整体流程与 SimpleProgressChecker 相同，但 <code>isRobotMovedEnough()</code> 改为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pose_distance(current, baseline) &gt; required_movement_radius</span></span>\n<span class="line"><span>or</span></span>\n<span class="line"><span>poseAngleDistance(current, baseline) &gt; required_movement_angle</span></span></code></pre></div><p>只要平移或旋转任一条件成立，就刷新同一组基准位姿和基准时间。这样，执行 RotateToHeading、窄通道转向或原地调整航向时，机器人可以用角度变化证明控制过程仍在推进。</p><h3 id="_3-3-poseangledistance-处理了正负-π-环绕" tabindex="-1">3.3 <code>poseAngleDistance()</code> 处理了正负 π 环绕 <a class="header-anchor" href="#_3-3-poseangledistance-处理了正负-π-环绕" aria-label="Permalink to &quot;3.3 `poseAngleDistance()` 处理了正负 π 环绕&quot;">​</a></h3><p>函数调用 <code>angles::shortest_angular_distance()</code>，再取绝对值。机器人从接近 <code>+π</code> 转到接近 <code>-π</code> 时，实际只转过一个很小角度；直接相减会得到接近 <code>2π</code> 的错误结果，最短角距离则会返回真实的小角差。</p><h3 id="_3-4-选择-pose-并不等于取消卡滞保护" tabindex="-1">3.4 选择 Pose 并不等于取消卡滞保护 <a class="header-anchor" href="#_3-4-选择-pose-并不等于取消卡滞保护" aria-label="Permalink to &quot;3.4 选择 Pose 并不等于取消卡滞保护&quot;">​</a></h3><p>PoseProgressChecker 只是把有效进展条件从“只允许平移”扩展成“平移或旋转”，超时逻辑仍然存在。如果机器人只在阈值以内来回抖动，既没有累计足够 XY 位移，也没有累计足够最短角变化，时间窗结束后依然会失败。</p><h2 id="四、三个-goalchecker-到底差在哪里" tabindex="-1">四、三个 GoalChecker 到底差在哪里 <a class="header-anchor" href="#四、三个-goalchecker-到底差在哪里" aria-label="Permalink to &quot;四、三个 GoalChecker 到底差在哪里&quot;">​</a></h2><h3 id="_4-1-position、simple、stopped-是三种不同终点契约" tabindex="-1">4.1 Position、Simple、Stopped 是三种不同终点契约 <a class="header-anchor" href="#_4-1-position、simple、stopped-是三种不同终点契约" aria-label="Permalink to &quot;4.1 Position、Simple、Stopped 是三种不同终点契约&quot;">​</a></h3><p>三个插件都实现 <code>nav2_core::GoalChecker</code>，但判断维度不同：</p><ul><li>PositionGoalChecker：检查 XY，忽略 Yaw，忽略速度。</li><li>SimpleGoalChecker：检查 XY 与 Yaw，忽略速度。</li><li>StoppedGoalChecker：继承 SimpleGoalChecker 的 XY 与 Yaw，再检查二维线速度和 Z 轴角速度。</li></ul><p>StoppedGoalChecker 继承 SimpleGoalChecker；PositionGoalChecker 是独立实现，并不是 SimpleGoalChecker 的简化开关。它们都接收 Costmap 指针以满足接口，但当前三个实现都不依赖 Costmap 内容。</p><h3 id="_4-2-gettolerances-报告的是插件实际使用字段" tabindex="-1">4.2 <code>getTolerances()</code> 报告的是插件实际使用字段 <a class="header-anchor" href="#_4-2-gettolerances-报告的是插件实际使用字段" aria-label="Permalink to &quot;4.2 `getTolerances()` 报告的是插件实际使用字段&quot;">​</a></h3><p>GoalChecker 接口允许 Controller 或其他调用方查询位姿与速度容差。实现会把未参与判定的字段设为 <code>std::numeric_limits&lt;double&gt;::lowest()</code> 之类的无效哨兵值。</p><p>PositionGoalChecker 返回单位四元数，只是为了填充合法消息字段，不代表它会检查朝向。SimpleGoalChecker 返回 XY 和 Yaw 容差，但速度字段无效。StoppedGoalChecker 在父类结果上补充 <code>linear.x</code>、<code>linear.y</code> 和 <code>angular.z</code> 的停止阈值。</p><p><img src="'+c+'" alt="图 3：三个 GoalChecker 的继承关系与判定维度"></p><h2 id="五、positiongoalchecker-只要进入目标圆就算完成" tabindex="-1">五、PositionGoalChecker：只要进入目标圆就算完成 <a class="header-anchor" href="#五、positiongoalchecker-只要进入目标圆就算完成" aria-label="Permalink to &quot;五、PositionGoalChecker：只要进入目标圆就算完成&quot;">​</a></h2><h3 id="_5-1-构造、析构与初始化" tabindex="-1">5.1 构造、析构与初始化 <a class="header-anchor" href="#_5-1-构造、析构与初始化" aria-label="Permalink to &quot;5.1 构造、析构与初始化&quot;">​</a></h3><p>构造函数把 <code>xy_goal_tolerance_</code> 设为 <code>0.25 m</code>，同时建立 <code>0.0625</code> 的平方缓存，并初始化 <code>stateful_=true</code> 与 <code>position_reached_=false</code>。平方缓存让高频目标检查可以直接比较平方距离，避免每周期计算平方根。</p><p>析构函数使用默认实现。插件不拥有独立节点或 Costmap 资源，参数回调句柄等成员由对象生命周期自动释放。</p><p><code>initialize()</code> 声明并读取 <code>xy_goal_tolerance</code> 与 <code>stateful</code>，同步计算平方容差并注册动态回调。传入的 <code>costmap_ros</code> 没有被使用，因为判定只需要当前和目标的 XY 坐标。</p><h3 id="_5-2-isgoalreached-只比较一个容差圆" tabindex="-1">5.2 <code>isGoalReached()</code> 只比较一个容差圆 <a class="header-anchor" href="#_5-2-isgoalreached-只比较一个容差圆" aria-label="Permalink to &quot;5.2 `isGoalReached()` 只比较一个容差圆&quot;">​</a></h3><p>核心条件为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>dx = current.x - goal.x</span></span>\n<span class="line"><span>dy = current.y - goal.y</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>dx² + dy² &lt;= xy_goal_tolerance²</span></span></code></pre></div><p>它不读取两个 Pose 的 orientation，也不读取传入的 Twist。机器人高速穿过容差圆时，只要某个检查周期采样到位置在圆内，就可以返回 <code>true</code>。</p><h3 id="_5-3-stateful-锁存的是最终成功" tabindex="-1">5.3 <code>stateful</code> 锁存的是最终成功 <a class="header-anchor" href="#_5-3-stateful-锁存的是最终成功" aria-label="Permalink to &quot;5.3 `stateful` 锁存的是最终成功&quot;">​</a></h3><p><code>stateful=true</code> 时，一旦位置条件成立，<code>position_reached_</code> 就被置为 <code>true</code>，之后即使定位抖动让机器人暂时离开容差圆，插件仍返回成功，直到 Controller Server 为新路径调用 <code>reset()</code>。</p><p><code>stateful=false</code> 时没有锁存，每个控制周期都会重新计算位置误差。<code>setXYGoalTolerance()</code> 和动态参数回调都会同步维护平方缓存，但不会清除已经锁存的成功状态。</p><h3 id="_5-4-它适合-到点即可-不适合-到点停正" tabindex="-1">5.4 它适合“到点即可”，不适合“到点停正” <a class="header-anchor" href="#_5-4-它适合-到点即可-不适合-到点停正" aria-label="Permalink to &quot;5.4 它适合“到点即可”，不适合“到点停正”&quot;">​</a></h3><p>巡检点只要求机器人经过某个区域、目标朝向没有业务含义时，PositionGoalChecker 可以减少无意义的末端旋转。如果任务要求机器人面向货架、充电桩或窄通道，它会过早结束；如果任务要求机器人真正停稳，它同样不够。</p><h2 id="六、simplegoalchecker-位置与朝向之间还有一个状态机" tabindex="-1">六、SimpleGoalChecker：位置与朝向之间还有一个状态机 <a class="header-anchor" href="#六、simplegoalchecker-位置与朝向之间还有一个状态机" aria-label="Permalink to &quot;六、SimpleGoalChecker：位置与朝向之间还有一个状态机&quot;">​</a></h2><h3 id="_6-1-构造与初始化建立四类规则" tabindex="-1">6.1 构造与初始化建立四类规则 <a class="header-anchor" href="#_6-1-构造与初始化建立四类规则" aria-label="Permalink to &quot;6.1 构造与初始化建立四类规则&quot;">​</a></h3><p>构造函数设置默认 <code>xy_goal_tolerance=0.25 m</code>、<code>yaw_goal_tolerance=0.25 rad</code>、<code>stateful=true</code>、<code>check_xy=true</code> 与 <code>symmetric_yaw_tolerance=false</code>。<code>initialize()</code> 声明并读取这些参数，计算 XY 平方缓存并注册动态回调。</p><h3 id="_6-2-isgoalreached-先过位置门-再过朝向门" tabindex="-1">6.2 <code>isGoalReached()</code> 先过位置门，再过朝向门 <a class="header-anchor" href="#_6-2-isgoalreached-先过位置门-再过朝向门" aria-label="Permalink to &quot;6.2 `isGoalReached()` 先过位置门，再过朝向门&quot;">​</a></h3><p>当 <code>check_xy_</code> 为 <code>true</code> 时，函数先比较 XY 平方距离。位置不满足就立即返回 <code>false</code>；位置满足后，如果 <code>stateful=true</code>，函数把 <code>check_xy_</code> 改为 <code>false</code>，后续周期不再重复检查 XY。</p><p>随后函数从两个四元数提取 Yaw，并用最短角距离计算偏航误差。只有误差绝对值小于等于 <code>yaw_goal_tolerance</code> 才返回 <code>true</code>。</p><h3 id="_6-3-stateful-锁存的是位置阶段-不是整个结果" tabindex="-1">6.3 <code>stateful</code> 锁存的是位置阶段，不是整个结果 <a class="header-anchor" href="#_6-3-stateful-锁存的是位置阶段-不是整个结果" aria-label="Permalink to &quot;6.3 `stateful` 锁存的是位置阶段，不是整个结果&quot;">​</a></h3><p>这与 PositionGoalChecker 的锁存语义不同。SimpleGoalChecker 在位置首次满足后仍可能因为 Yaw 不满足而返回 <code>false</code>；<code>stateful=true</code> 只是允许机器人在后续调整朝向时稍微漂出 XY 容差，而不重新退回位置阶段。</p><p><code>reset()</code> 把 <code>check_xy_</code> 恢复为 <code>true</code>。动态修改 <code>stateful</code> 或 XY 容差不会自动调用 <code>reset()</code>，所以运行中把 <code>stateful</code> 从 <code>true</code> 改为 <code>false</code>，并不会自动把已经关闭的 <code>check_xy_</code> 重新打开。</p><h3 id="_6-4-symmetric-yaw-tolerance-接受相反朝向" tabindex="-1">6.4 <code>symmetric_yaw_tolerance</code> 接受相反朝向 <a class="header-anchor" href="#_6-4-symmetric-yaw-tolerance-接受相反朝向" aria-label="Permalink to &quot;6.4 `symmetric_yaw_tolerance` 接受相反朝向&quot;">​</a></h3><p>默认情况下，只有目标 Yaw 附近的角度可接受。开启 <code>symmetric_yaw_tolerance</code> 后，代码还会计算当前 Yaw 到 <code>goal_yaw + π</code> 的最短角距离，两种朝向任一落入容差都算成功。</p><p>这个选项适合前后外形和任务语义等价的机器人，但不适合相机、机械臂、叉齿或充电触点有明确朝向要求的系统。它不是把 Yaw 检查关闭，而是把一个可接受方向扩展成两个相差 π 的方向。</p><h3 id="_6-5-gettolerances-不会凭空加入速度条件" tabindex="-1">6.5 <code>getTolerances()</code> 不会凭空加入速度条件 <a class="header-anchor" href="#_6-5-gettolerances-不会凭空加入速度条件" aria-label="Permalink to &quot;6.5 `getTolerances()` 不会凭空加入速度条件&quot;">​</a></h3><p>函数报告 XY 与 Yaw 容差，并把所有速度字段设为无效。传入 <code>isGoalReached()</code> 的 Twist 也被明确忽略。因此“用了 SimpleGoalChecker，所以到点一定停稳”是错误结论。</p><p><img src="'+r+'" alt="图 4：SimpleGoalChecker 的 stateful 位置阶段与偏航阶段"></p><h2 id="七、stoppedgoalchecker-几何到点之后还必须停稳" tabindex="-1">七、StoppedGoalChecker：几何到点之后还必须停稳 <a class="header-anchor" href="#七、stoppedgoalchecker-几何到点之后还必须停稳" aria-label="Permalink to &quot;七、StoppedGoalChecker：几何到点之后还必须停稳&quot;">​</a></h2><h3 id="_7-1-构造与初始化复用父类规则" tabindex="-1">7.1 构造与初始化复用父类规则 <a class="header-anchor" href="#_7-1-构造与初始化复用父类规则" aria-label="Permalink to &quot;7.1 构造与初始化复用父类规则&quot;">​</a></h3><p>构造函数先建立 SimpleGoalChecker 的位置和偏航状态，再把 <code>rot_stopped_velocity_</code> 与 <code>trans_stopped_velocity_</code> 都设为 <code>0.25</code>。单位分别是 <code>rad/s</code> 和 <code>m/s</code>。</p><p><code>initialize()</code> 先调用父类初始化，再声明并读取 <code>rot_stopped_velocity</code> 与 <code>trans_stopped_velocity</code>，最后注册子类动态回调。父类回调继续负责位姿参数，子类回调只负责速度参数。</p><h3 id="_7-2-isgoalreached-是串联条件-不是替代条件" tabindex="-1">7.2 <code>isGoalReached()</code> 是串联条件，不是替代条件 <a class="header-anchor" href="#_7-2-isgoalreached-是串联条件-不是替代条件" aria-label="Permalink to &quot;7.2 `isGoalReached()` 是串联条件，不是替代条件&quot;">​</a></h3><p>函数先调用 <code>SimpleGoalChecker::isGoalReached()</code>。如果 XY 或 Yaw 不成立，立即返回 <code>false</code>；父类成立后，再检查：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>hypot(velocity.linear.x, velocity.linear.y)</span></span>\n<span class="line"><span>    &lt;= trans_stopped_velocity</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>abs(velocity.angular.z)</span></span>\n<span class="line"><span>    &lt;= rot_stopped_velocity</span></span></code></pre></div><p>线速度使用二维模长，而不是只看 <code>linear.x</code>。例如 <code>vx</code> 和 <code>vy</code> 单独都低于阈值，并不保证合速度低于阈值。角速度只读取平面导航使用的 <code>angular.z</code>。</p><h3 id="_7-3-速度来自阈值化里程计-不是刚发布的命令" tabindex="-1">7.3 速度来自阈值化里程计，不是刚发布的命令 <a class="header-anchor" href="#_7-3-速度来自阈值化里程计-不是刚发布的命令" aria-label="Permalink to &quot;7.3 速度来自阈值化里程计，不是刚发布的命令&quot;">​</a></h3><p>Controller Server 调用 GoalChecker 前，会读取里程计 Twist 并应用 <code>min_x_velocity_threshold</code>、<code>min_y_velocity_threshold</code> 与 <code>min_theta_velocity_threshold</code>。StoppedGoalChecker 看到的是这份处理后的反馈速度，不是局部控制器刚发布的速度命令。</p><p>这意味着停止判定同时受 Odom 数据质量和 Controller Server 最小速度阈值影响。最小阈值设得过大，真实的小速度可能先被归零，使 StoppedGoalChecker 更早成功；里程计噪声过大，则可能让机器人机械上已经停住但插件迟迟不通过。</p><h3 id="_7-4-gettolerances-与动态回调补齐速度维度" tabindex="-1">7.4 <code>getTolerances()</code> 与动态回调补齐速度维度 <a class="header-anchor" href="#_7-4-gettolerances-与动态回调补齐速度维度" aria-label="Permalink to &quot;7.4 `getTolerances()` 与动态回调补齐速度维度&quot;">​</a></h3><p><code>getTolerances()</code> 先调用父类填充 XY 与 Yaw，再把平移阈值写入 <code>linear.x</code>、<code>linear.y</code>，把旋转阈值写入 <code>angular.z</code>。动态回调更新两个 double 阈值，同样不会重置父类的 <code>check_xy_</code> 状态。</p><p><img src="'+n+'" alt="图 5：StoppedGoalChecker 的位姿条件与停稳速度条件"></p><h2 id="八、动态参数-改的是阈值-不是插件历史" tabindex="-1">八、动态参数：改的是阈值，不是插件历史 <a class="header-anchor" href="#八、动态参数-改的是阈值-不是插件历史" aria-label="Permalink to &quot;八、动态参数：改的是阈值，不是插件历史&quot;">​</a></h2><h3 id="_8-1-父类和子类回调各管一组参数" tabindex="-1">8.1 父类和子类回调各管一组参数 <a class="header-anchor" href="#_8-1-父类和子类回调各管一组参数" aria-label="Permalink to &quot;8.1 父类和子类回调各管一组参数&quot;">​</a></h3><p>PoseProgressChecker 与 StoppedGoalChecker 都先注册父类回调，再注册子类回调。参数更新请求到来时，多个回调分别检查参数类型和完整名称，只消费自己负责的字段。</p><ul><li>SimpleProgressChecker：<code>required_movement_radius</code>、<code>movement_time_allowance</code>。</li><li>PoseProgressChecker：<code>required_movement_angle</code>。</li><li>PositionGoalChecker：<code>xy_goal_tolerance</code>、<code>stateful</code>。</li><li>SimpleGoalChecker：<code>xy_goal_tolerance</code>、<code>yaw_goal_tolerance</code>、<code>stateful</code>、<code>symmetric_yaw_tolerance</code>。</li><li>StoppedGoalChecker：<code>trans_stopped_velocity</code>、<code>rot_stopped_velocity</code>。</li></ul><h3 id="_8-2-所有更新都不会自动-reset" tabindex="-1">8.2 所有更新都不会自动 <code>reset()</code> <a class="header-anchor" href="#_8-2-所有更新都不会自动-reset" aria-label="Permalink to &quot;8.2 所有更新都不会自动 `reset()`&quot;">​</a></h3><p>进展检查器会保留当前基准位姿和基准时间；PositionGoalChecker 会保留 <code>position_reached_</code>；SimpleGoalChecker 与 StoppedGoalChecker 会保留 <code>check_xy_</code>。因此动态调参改变的是从下一次检查开始使用的规则，不会自动清空任务历史。</p><h3 id="_8-3-当前回调没有额外拒绝负阈值" tabindex="-1">8.3 当前回调没有额外拒绝负阈值 <a class="header-anchor" href="#_8-3-当前回调没有额外拒绝负阈值" aria-label="Permalink to &quot;8.3 当前回调没有额外拒绝负阈值&quot;">​</a></h3><p>源码按类型和名称更新成员，并返回 <code>successful=true</code>，没有在这些回调里额外拒绝负半径、负时间、负位置容差或负速度阈值。负容差通常会制造永远无法满足或立即超时的语义，参数管理侧不应依赖插件替你兜底。</p><p><img src="'+d+`" alt="图 6：动态参数回调分工与两组测试覆盖"></p><h2 id="九、怎样选插件-怎样排查误判" tabindex="-1">九、怎样选插件，怎样排查误判 <a class="header-anchor" href="#九、怎样选插件-怎样排查误判" aria-label="Permalink to &quot;九、怎样选插件，怎样排查误判&quot;">​</a></h2><h3 id="_9-1-包含原地转向的控制过程" tabindex="-1">9.1 包含原地转向的控制过程 <a class="header-anchor" href="#_9-1-包含原地转向的控制过程" aria-label="Permalink to &quot;9.1 包含原地转向的控制过程&quot;">​</a></h3><p>如果局部控制器会执行明显的原地旋转阶段，优先使用 PoseProgressChecker，让角度变化也能刷新进展时间窗。配置示例为：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">controller_server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  ros__parameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    progress_checker_plugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;progress_checker&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    progress_checker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      plugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nav2_controller::PoseProgressChecker&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      required_movement_radius</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.50</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      required_movement_angle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.50</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      movement_time_allowance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10.0</span></span></code></pre></div><p>如果机器人只应通过平移证明任务推进，SimpleProgressChecker 更直接。不要只把 <code>movement_time_allowance</code> 无限调大来掩盖误判，那会同时削弱真实卡滞的发现能力。</p><h3 id="_9-2-只要求经过目标区域" tabindex="-1">9.2 只要求经过目标区域 <a class="header-anchor" href="#_9-2-只要求经过目标区域" aria-label="Permalink to &quot;9.2 只要求经过目标区域&quot;">​</a></h3><p>目标朝向无意义、任务只要求进入某个 XY 区域时，可以使用 PositionGoalChecker：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">controller_server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  ros__parameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    goal_checker_plugins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;position_goal_checker&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    position_goal_checker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      plugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nav2_controller::PositionGoalChecker&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      xy_goal_tolerance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      stateful</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span></code></pre></div><p>这份配置不会检查 Yaw 和速度，必须接受“进入容差圆就结束”的真实语义。</p><h3 id="_9-3-要求停在目标姿态" tabindex="-1">9.3 要求停在目标姿态 <a class="header-anchor" href="#_9-3-要求停在目标姿态" aria-label="Permalink to &quot;9.3 要求停在目标姿态&quot;">​</a></h3><p>任务要求位置、朝向和停稳同时成立时，使用 StoppedGoalChecker：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">controller_server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  ros__parameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    goal_checker_plugins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stopped_goal_checker&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    stopped_goal_checker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      plugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nav2_controller::StoppedGoalChecker&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      xy_goal_tolerance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      yaw_goal_tolerance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      stateful</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      symmetric_yaw_tolerance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      trans_stopped_velocity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.05</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      rot_stopped_velocity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.05</span></span></code></pre></div><p>示例中的停止阈值只是语义演示，不能脱离底盘低速能力、里程计噪声和 Controller Server 的最小速度阈值直接照搬。</p><h3 id="_9-4-出现-failed-to-make-progress-时看什么" tabindex="-1">9.4 出现 <code>Failed to make progress</code> 时看什么 <a class="header-anchor" href="#_9-4-出现-failed-to-make-progress-时看什么" aria-label="Permalink to &quot;9.4 出现 \`Failed to make progress\` 时看什么&quot;">​</a></h3><ul><li>当前加载的是 SimpleProgressChecker 还是 PoseProgressChecker。</li><li>控制器是否正在原地旋转，而插件只把 XY 位移视为进展。</li><li>当前基准窗口内的累计位移、累计最短角变化和经过时间。</li><li><code>required_movement_radius</code>、<code>required_movement_angle</code> 与定位噪声、机器人真实运动尺度是否匹配。</li><li>运行期是否刚缩短 <code>movement_time_allowance</code> 或调大移动阈值，却没有重置当前窗口。</li></ul><h3 id="_9-5-到点后-followpath-不结束时看什么" tabindex="-1">9.5 到点后 FollowPath 不结束时看什么 <a class="header-anchor" href="#_9-5-到点后-followpath-不结束时看什么" aria-label="Permalink to &quot;9.5 到点后 FollowPath 不结束时看什么&quot;">​</a></h3><ul><li>当前 Action 选中的 <code>goal_checker_id</code> 是否对应预期插件。</li><li>XY 距离、最短 Yaw 误差分别卡在哪个条件。</li><li><code>stateful</code> 是否让 XY 阶段已经锁存，或者运行期改参后内部状态仍沿用旧任务历史。</li><li>StoppedGoalChecker 看到的阈值化 Odom Twist 是否仍超过二维线速度或角速度阈值。</li><li><code>symmetric_yaw_tolerance</code> 是否符合任务方向语义，不能为了结束任务随意开启。</li></ul><h3 id="_9-6-两组-gtest-钉住了哪些边界" tabindex="-1">9.6 两组 GTest 钉住了哪些边界 <a class="header-anchor" href="#_9-6-两组-gtest-钉住了哪些边界" aria-label="Permalink to &quot;9.6 两组 GTest 钉住了哪些边界&quot;">​</a></h3><p><code>pctest</code> 覆盖首次基准、重置、允许时间内与超时后的返回值、平移阈值、旋转阈值和动态修改允许时间。<code>gctest</code> 覆盖 Simple 与 Stopped 的差异、容差查询、动态参数、二维合速度、角速度、阈值附近浮点边界和对称偏航。</p><p>当前源码的既有本地验证记录为 <code>pctest</code> 与 <code>gctest</code> 全部通过，<code>cppcheck</code>、<code>lint_cmake</code>、<code>xmllint</code> 也通过。本文任务没有修改或重新构建插件源码，文章结论以当前 checkout 和既有测试证据为边界。</p><h2 id="十、总结-把-过程健康-和-任务完成-分开调" tabindex="-1">十、总结：把“过程健康”和“任务完成”分开调 <a class="header-anchor" href="#十、总结-把-过程健康-和-任务完成-分开调" aria-label="Permalink to &quot;十、总结：把“过程健康”和“任务完成”分开调&quot;">​</a></h2><h3 id="_10-1-progresschecker-不负责到点" tabindex="-1">10.1 ProgressChecker 不负责到点 <a class="header-anchor" href="#_10-1-progresschecker-不负责到点" aria-label="Permalink to &quot;10.1 ProgressChecker 不负责到点&quot;">​</a></h3><p>SimpleProgressChecker 用 XY 位移刷新时间窗；PoseProgressChecker 额外接受最短角变化。它们的 <code>false</code> 表示过程在配置窗口内没有达到进展阈值，不表示目标一定不可达，也不表示控制器没有输出速度。</p><h3 id="_10-2-goalchecker-不负责生成速度" tabindex="-1">10.2 GoalChecker 不负责生成速度 <a class="header-anchor" href="#_10-2-goalchecker-不负责生成速度" aria-label="Permalink to &quot;10.2 GoalChecker 不负责生成速度&quot;">​</a></h3><p>PositionGoalChecker 定义“到位置”；SimpleGoalChecker 定义“到位置并对齐朝向”；StoppedGoalChecker 定义“到位置、对齐朝向并满足反馈速度阈值”。选择插件，本质上是在选择任务完成契约。</p><h3 id="_10-3-状态与阈值必须一起看" tabindex="-1">10.3 状态与阈值必须一起看 <a class="header-anchor" href="#_10-3-状态与阈值必须一起看" aria-label="Permalink to &quot;10.3 状态与阈值必须一起看&quot;">​</a></h3><p><code>baseline_pose_set_</code>、<code>position_reached_</code> 和 <code>check_xy_</code> 会跨越多个控制周期保存历史，而动态参数只改阈值、不自动重置。现场调试不能只截一帧参数或一帧 Pose，必须同时还原插件类型、当前内部阶段、位姿误差、速度反馈和时间窗口。</p><p>理解这 5 个插件后，再遇到“机器人明明在动却被判卡住”或“机器人明明到了却不结束”，就不必盲目放宽所有容差。先确认是哪一道检查在做决定，再对准它真正使用的数据和状态。</p>`,135)])])}const C=a(h,[["render",p]]);export{b as __pageData,C as default};
